# 개발 진행 상황 및 계획

> 마지막 업데이트: 2026.07.12

---

## 🆕 최근 작업 (2026.07.12, Claude와 진행 (4)) — v0.4.12 배포

### 산 실루엣 다양화
`design-style.md`에 설계만 있고 코드 미반영 상태였던 걸 구현. 상세: `design-style.md`
"산 모양 다양화"

- [x] `getMountainShape(book)` — 책 제목 12자 이상이면 쌍봉(twin), 그 외엔 책 id
  시드로 뾰족(sharp)/비대칭(skew)/고원(plateau) 중 하나
- [x] `getMountainProfile(shape, steps, seed)` — 컬럼별 높이맵(`MountainProfile`)으로
  일반화. `drawMountainBody()`가 라이브 렌더·PNG 파노라마·hit-test(`getMountainRects`)
  전부에서 이 프로필을 공유해 어긋남 없음

### 바이럴 캡처 (정상 인증샷)
`docs/features/viral-capture.md`의 열린 질문 3개(트리거/폭죽 피크 프레임/워터마크)를
결정하고 구현. 상세: `docs/features/viral-capture.md`

- [x] `renderCompletionCapture()` — 산책기록의 1:1 정사각형 "정상 인증샷". 방금
  완독한 산을 중심에, 옆에 다른 책 최대 3권을 축소·반투명 실제 산으로 곁들임
  (`drawSideMountain()`), CLEAR!+폭죽(피크 프레임 2000ms 고정, book id 시드)+정상
  댄스 캐릭터, KDC 색 뱃지(`drawKdcBadge`), 워터마크("산책또산책 | 닉네임")
- [x] 트리거: 완독 세레모니 유예시간(`COMPLETION_GRACE_MS`) 중 좌하단 카메라
  버튼이 "인증샷 찍기"로 전환(`SlideToCapture.tsx`, 아이폰 "밀어서 잠금해제" 비주얼
  — 처음엔 산 위에 별도 버튼을 띄웠다가 메모 말풍선과 겹쳐 좌하단 기존 버튼에 통합,
  드래그 조작도 번거롭다는 피드백으로 탭 한 번으로 단순화)
- [x] 산 크기·비율은 "하늘 여백이 너무 크다" ↔ "산이 너무 크다" 피드백을 여러 차례
  주고받으며 조정 — 최종적으로 산 정상이 캔버스 세로 1/2 지점에 닿는 목표치의 3/4만
  적용, 같은 배율(zoom)을 옆산·해에도 나눠줘서 장면 전체가 함께 커지도록 함. 옆산은
  가로 폭 상한 없이 캔버스 밖으로 나가면 그대로 잘리는 구도를 허용
  - [x] 폭죽이 터지는 산은 메모 말풍선 후보에서 제외(어수선함 방지)
  - [x] 산-책 제목 사이 간격 소폭 확대(닿지 않을 정도로만)
- [x] 산책기록(home) 카메라 기본 동작 분리 — 완등기록(trophy)은 기존대로 "완독
  맵"(완독한 책만 모은 파노라마) 저장, 산책기록은 지금 화면을 `canvas.toDataURL()`로
  그대로 저장하는 "산책기록 월드맵"으로 분리(예전엔 모드 무관하게 항상 완독맵만
  나와서 혼란스럽다는 피드백)

### 그 외 UI 수정
- [x] 프로필 정보(`ProfileTrigger`)를 "산책기록"/"완등기록" 제목과 같은 줄로 정렬,
  시간대별 인사 문구(2시간 단위 12구간) 추가. 모바일 좁은 화면에선 제목과 겹쳐
  보인다는 피드백으로 인사 문구는 생략하고 닉네임만 노출

---

## 🆕 최근 작업 (2026.07.12, Claude와 진행 (3)) — v0.3.2 배포

### 산책자 증표 (프로필)
"구글 이름만 보여주는 인사말이면 수정할 방법이 있어야 하지 않냐"는 질문에서
시작 — 논의 끝에 인라인 편집 대신 별도 팝업으로 구조를 바꿈. 상세: `docs/features/profile.md`

- [x] 트리거: 오른쪽 상단 "{닉네임}님, 반가워요" + 사람 아이콘 버튼
  (`components/dashboard/ProfileTrigger.tsx`)
- [x] 팝업(`components/dashboard/ProfileModal.tsx`, 공용 `Modal.tsx` 재사용): 닉네임
  입력/저장(`updateNickname()`), 산책 시작일(`user.created_at`), 최근 산책일
  (`status_changed_at` 최댓값), 통계 카드 4개(내가 산 책/발걸음 수/완등기록/완등거리)
- [x] 산책기록·완등기록 데이터를 모두 보여줘야 해서 `app/dashboard/layout.tsx`에서
  공통 렌더링(양쪽 페이지 모두 노출)
- [x] `updateProgress()` 확장 — 상태 변화 없이 페이지만 저장해도 `status_changed_at`
  갱신하도록 바꿔서 "최근 산책일" 정확도 확보 (부수 효과: 목록 정렬도 더 정확해짐)
- [x] `DISTANCE_PER_PAGE_M`을 `worldmap-utils.ts`로 이동 — 완등기록 페이지·프로필
  팝업의 거리 숫자 통일
- [x] 명칭: "프로필"(딱딱함) → "산책자 증표"로 확정. "가입일" → "산책 시작일"로 문구 조정
- [x] 기존 `Greeting.tsx`(시간대별 인사말 + 연필 아이콘 인라인 편집)는 폐기 —
  `docs/features/nickname.md`에 대체 안내 남김

---

## 🆕 최근 작업 (2026.07.12, Claude와 진행 (2)) — v0.3.1 배포

### 닉네임 & 인사말
`docs/features/viral-capture.md`의 "닉네임 설정 미구현" 블로커 해소. 상세: `docs/features/nickname.md`

- [x] 저장 위치: `auth.users.user_metadata.nickname` (새 테이블 없음, `updateNickname()`
  서버 액션 — `app/dashboard/account-actions.ts`)
- [x] 기본값: 직접 설정한 닉네임 → 구글 프로필 이름(`full_name`/`name`) → `'산책자'`
- [x] UI: `components/dashboard/Greeting.tsx` — 산책기록(`/dashboard`)에서만 `AddBookBar`
  위에 시간대별 인사말(새벽/주간/황혼/야간, `WorldMap` 하늘 구간과 동일 경계) 노출.
  편집은 `BookCard`와 동일한 연필 아이콘 인라인 패턴(별도 모달 없음)
- [x] 완등기록 페이지에는 `nickname` prop을 안 넘겨서 자연스럽게 인사말 미노출

---

## 🆕 최근 작업 (2026.07.12, Claude와 진행) — v0.3.0 배포

### 배경
"진입 페이지가 제목 + 로그인 버튼뿐이라 어떤 서비스인지 모른 채 로그인부터 해야
한다"는 피드백에서 시작. 여러 차례 방향을 조율한 끝에 "메인 화면(예시 지형도)을
먼저 보여주고, 실제 쓰기 액션에서만 로그인을 요구"하는 구조로 확정.

### 진입 플로우 개편
- [x] `app/page.tsx` — 로그인 여부와 무관하게 항상 `/dashboard`로 리다이렉트 (기존:
  비로그인이면 `/login`으로 보냄)
- [x] `middleware.ts` — `protectedPaths`에서 `/dashboard` 제거. 실제 데이터 접근
  제어는 각 페이지(`app/dashboard/page.tsx`, `app/dashboard/hikes/page.tsx`) 내부에서
  `user` 유무로 직접 분기
- [x] `lib/demo-books.ts` 신규 — 비로그인 산책기록에 보여줄 예시 책 4권
  (`DEMO_HOME_BOOKS`, 여러 KDC 분야에 미시작/독서중 섞음)
- [x] `app/dashboard/page.tsx` — `user`가 없으면 실제 DB 조회 없이 `DEMO_HOME_BOOKS`로
  월드맵 렌더링. 계정 데이터를 직접 다루는(삭제·메모 등) 하단 책 목록(`BookCard`)은
  비로그인일 땐 아예 안 보여줌
- [x] `app/dashboard/hikes/page.tsx` — 처음엔 가짜 완독 예시(`DEMO_TROPHY_BOOKS`)를
  만들었다가, "완등한 책이 이곳에 없는데 있는 것처럼 보이면 안 된다"는 피드백으로
  폐기. 최종적으로는 로그인 유저가 완독 0권일 때 보는 것과 **완전히 동일한 빈 상태
  화면**(지도 안내 문구 + `EmptyState`)을 그대로 재사용하는 방식으로 정리 —
  `DEMO_TROPHY_BOOKS`는 사용처가 없어져 `lib/demo-books.ts`에서 삭제

### 로그인 유도 (책 추가 / 게이지 저장 / CSV / 회원 탈퇴)
- [x] `lib/auth/signInWithGoogle.ts` 신규 — `app/login/page.tsx`의 구글 로그인 로직을
  공용 함수로 분리
- [x] `WorldMapClient`/`AddBookBar`/`AddBookForm`에 `authenticated` prop 체인 추가 —
  비로그인 상태에서 "책 추가" 버튼(하단 버튼 + 하늘 해/별 버튼 둘 다)을 누르면 폼을
  열지 않고 바로 구글 로그인 트리거
- [x] `ProgressModal`도 `authenticated` prop 추가 — 비로그인이어도 게이지 드래그
  체험은 되지만, "저장" 버튼을 누르면 실제 저장 대신 구글 로그인으로 유도
- [x] `Sidebar`의 CSV 내려받기 / 회원 탈퇴도 같은 패턴 — 비로그인이면 클릭 시
  구글 로그인
- [x] `LogoutButton`/`DeleteAccountModal` — 로그아웃·탈퇴 후 이동 경로를 `/login`
  대신 예시 지형도가 있는 `/dashboard`로 변경 (처음엔 `/login`으로 보내서, 로그아웃하면
  다시 옛날 진입 화면이 보이는 버그가 있었음)

### 사이드바 메뉴 개편
- [x] 순서를 "산책또산책에 대하여 → 로그인/로그아웃 → CSV 내려받기 →
  개인정보처리방침·이용약관 → 회원 탈퇴"로 재배열, 항목 사이 구분선 전부 제거
  (하단 버전 정보 위 구분선도 함께 제거)
- [x] 비로그인 상태에선 로그아웃 버튼 자리에 "로그인" 버튼 노출(클릭 시 바로 구글
  로그인)

### 월드맵 TUTORIAL 라벨
- [x] `WorldMap.tsx`에 `demo` prop 추가 — true면 하늘 중앙에 sin 파형으로 부드럽게
  반짝이는 "TUTORIAL" 픽셀 글씨(CLEAR! 텍스트와 같은 서체, 크기는 3배인 42px) +
  그 아래 "로그인 후 책을 추가해보세요" 안내를 오버레이로 그림
- [x] `WorldMapClient`가 `!authenticated`일 때만 `demo=true`로 전달 — 로그인
  상태/완등기록에는 노출 안 됨
- [x] 시행착오: "완등한 산이 이곳으로 옮겨옵니다" 문구를 캔버스 안(지도 하단)과
  페이지 하단 둘 다에 넣어봤다가, 산책기록 지도에는 예시 산이 이미 차 있어서 산과
  겹쳐 지저분해 보이는 문제로 **둘 다 제거** — 결과적으로 산책기록 화면엔 TUTORIAL
  라벨만 남음

### 문구 교체
- [x] 완등기록 빈 상태 문구 — "완독하면 여기에 정상석이 생겨요" →
  "완등한 산이 이곳으로 옮겨옵니다"로 교체 (`WorldMap.tsx`의 캔버스 안내문,
  `hikes/page.tsx`의 `EmptyState` subtitle 둘 다)
- [x] 예시 지형도 책 제목을 실제 도서 제목으로 교체 — 데미안→압록강은 흐른다,
  사피엔스→망고와 수류탄, 논어→제자리에 있다는 것, 침묵의 봄→랩걸,
  어린 왕자→소년이 온다 (완등기록 예시가 빈 상태 화면으로 바뀌며 이 제목들은
  현재 산책기록 예시에만 남음)

### 배포 관련 이슈
- ⚠️ **이 세션이 진행된 로컬(Claude Cowork) 샌드박스는 Linux(arm64)인데 프로젝트
  `node_modules`는 macOS용 `@next/swc-darwin-arm64`만 설치돼 있어 `next build`가
  아예 진행되지 않고 멈춤** — 로컬 검증은 `npx tsc --noEmit` + `npx eslint`로 대체
  (둘 다 타입/린트 오류 없음 확인). 실제 프로덕션 빌드는 Vercel(Linux)에서 정상 수행됨
- ⚠️ **GitHub push는 됐는데 Vercel이 한 번 웹훅을 놓쳐서 배포가 안 된 적 있었음** —
  다른 커밋들과 달리 GitHub 커밋 옆에 상태 체크(✓)가 안 붙어 있는 걸로 확인.
  빈 커밋(`git commit --allow-empty`)으로 재push해서 웹훅을 재트리거해 해결
- 관련 커밋: `54da006`, `248e89c`, `cc13f66`, `2127d8b`, `f6c64e7`(웹훅 재트리거),
  `eefff77`, `cf84a90`, `d4dc430`, `e28d3a3`, `9642287`, `b324434`, `d0f64fb`

---

## 🆕 최근 작업 (2026.07.12) — v0.2.1 배포

### KDC 필드 매핑 (`app/api/books/search/route.ts`, `AddBookForm.tsx`, `actions.ts`)
- [x] **SEOJI 응답에서 KDC/SUBJECT 추출** — `resolveKdc()`: `KDC`(정본) 우선, 없으면
  `SUBJECT`(대분류 한 자리 숫자)로 폴백. 실측 확인 결과 `KDC`는 CIP 사전등록 도서에서만
  채워지고 대부분은 비어 있어 `SUBJECT`가 사실상 주 데이터 소스.
- [x] **폼 → 서버 액션 전달 경로 연결** — `AddBookForm`에 hidden `kdc` 필드 추가,
  `addBook`이 이를 읽어 `books.kdc` 컬럼에 저장.
- [x] WorldMap의 `getTheme()`/DB 스키마는 이미 kdc를 지원하고 있어 추가 변경 불필요.
- [x] 관련 문서 갱신: `docs/api.md`, `docs/verification.md`(⚠️ 부분 6 → ✅ 해소).
- 상세 근거: `docs/verification.md`의 "✅ 해소 6" 참고.

---

## 🆕 최근 작업 (2026.07.11) — v0.2.0 배포

### 책 추가 팝업 버그 수정 (`components/books/AddBookForm.tsx`)
- [x] **검색 결과 목록 스크롤 안 되던 문제** — 결과 `<ul>`에 높이 제한이 없어 결과가 많으면 팝업 자체가 계속 늘어나던 버그. `max-h-72 overflow-y-auto` 적용해 목록 내부에서만 스크롤되도록 수정
- [x] **저자가 많은 책은 저자란이 한없이 길어지던 문제** — 검색 결과 선택/ISBN 스캔으로 저자 자동 입력할 때 `truncateAuthors()`로 30자 제한 적용. "선택된 책 미리보기" 카드의 저자 표시에도 동일하게 적용해서 두 곳 다 일관되게 30자로 잘림

### 베타 배지 + 버전 관리 도입
- [x] **`lib/version.ts` 신규** — `APP_VERSION`(package.json 버전 따라감), `LAST_UPDATED`(수동 갱신) 상수 관리
- [x] **`CHANGELOG.md` 신규** — 버전별 변경 이력 기록 시작 (0.1.0, 0.2.0)
- [x] **사이드바(`Sidebar.tsx`) 표기 추가** — "Read & Ridge" 부제 옆에 BETA 배지, 메뉴 하단(로그아웃/탈퇴 등과 구분선으로 분리된 별도 블록, `nav`가 `flex-1`이라 항상 사이드바 맨 아래에 고정)에 `v{APP_VERSION} · {LAST_UPDATED} 업데이트` 표시
- [x] `package.json` 버전 0.1.0 → 0.2.0
- 배포마다 `lib/version.ts`의 `LAST_UPDATED`와 `CHANGELOG.md`를 같이 갱신하기로 함

### 기타
- [x] **매달 이스터에그 선정 알림 스케줄 등록** — 매달 1일 오전 9시, 지난 이스터에그(오로라 효과 등)와 안 겹치는 새 아이디어 제안까지 포함해서 알림 오도록 설정 (`monthly-easter-egg-reminder`, 코드 작업 아님·리마인더 성격)

### 배포
- 위 변경사항 전부 커밋 `4d72202`로 묶여 `origin/main`에 push 완료, Vercel 자동 재배포 확인. (직전 세션에서 마운트된 로컬 저장소의 `.git/index.lock`이 반복적으로 삭제 권한 문제를 일으켜 커밋이 여러 번 실패했었음 — VS Code가 git 프로세스를 물고 있던 것으로 추정, VS Code 종료 후 해결)

---

## 🆕 열린 질문 메모 (2026.07.08)

### 정책 페이지 (2026.07.08)
- [x] **개인정보처리방침 페이지 신규 추가** (`/privacy`, `app/privacy/page.tsx`) — 로그인 없이도 접근 가능한 공개 라우트(middleware `protectedPaths`에 안 걸림). 수집 항목(구글 OAuth 이메일/이름/사진, 이용자가 입력하는 책 정보), 이용 목적, 보유기간, 제3자 제공(Supabase/Google/국립중앙도서관·Open Library API), 이용자 권리, 쿠키, 아동 보호, 안전성 확보 조치, 고지 의무 순으로 구성. 로그인 페이지(`/login`)와 사이드바 하단에 링크 추가
- [x] **문의처 이메일 확정** — `readandridge@gmail.com`으로 별도 계정 생성, "9. 문의처" 섹션에 반영 완료 (2026.07.08)
- [x] **저작권 표기 추가** — 사이드바 하단에 `© 2026 산책또산책` 표기, 개인정보처리방침 페이지 하단에도 도서 정보(국립중앙도서관/Open Library API 제공, 저작권은 각 출판사/저작권자에게 있음) 관련 저작권 안내 문구 추가
- [x] **버그 수정**: 개인정보처리방침에 실제 안 쓰는 "카카오 도서 검색 API"가 잘못 기재돼 있던 걸 발견 — 실제 도서 검색은 국립중앙도서관 API + Open Library라서 문구 전체(정책 페이지, `docs/architecture.md`, `docs/features/books.md`, `docs/verification.md`, `docs/product-direction.md`)에서 카카오 언급을 국립중앙도서관 API로 정정. (카카오는 로그인 방식 후보로만 검토됐다가 진행 안 함으로 결정된 것과는 별개 사안 — 혼동 주의)
- [x] **이용약관 페이지 신규 추가** (`/terms`, `app/terms/page.tsx`) — 개인정보처리방침과 동일하게 공개 라우트. 목적/정의/약관 효력·변경/서비스 제공·변경/회원 의무/콘텐츠 관리(이용자 등록 콘텐츠 권리는 본인에게 있음 명시)/서비스 중단/면책조항/회원 탈퇴/저작권 귀속/분쟁해결·준거법/문의처 순으로 구성. 개인정보처리방침·이용약관 페이지끼리 서로 링크, 사이드바·로그인 페이지에도 두 링크 나란히 추가
- [x] **회원 탈퇴(계정 완전 삭제) 기능 구현** — 사이드바 하단 "회원 탈퇴" 버튼 → `DeleteAccountModal`(실수 방지용 "탈퇴합니다" 타이핑 확인) → `app/dashboard/account-actions.ts`의 `deleteAccount()` 서버 액션이 (1) `books`/레거시 `hikes` 행 삭제 (2) 로그아웃 (3) `lib/supabase/admin.ts`의 서비스 롤 클라이언트로 Supabase Auth 계정 자체까지 완전 삭제. 로그아웃 버튼과 동일하게 클라이언트에서 `router.push('/login')`으로 이동시킴(서버 액션 안에서 `redirect()`를 쓰면 실패 시 에러 메시지를 보여주는 try/catch가 그 특수 예외까지 삼켜버릴 수 있어 일부러 피함)
  - ⚠️ **`SUPABASE_SERVICE_ROLE_KEY` 환경변수 설정 필요** — Supabase 프로젝트 대시보드 > Project Settings > API에서 `service_role` 키를 복사해 `.env.local`에 붙여넣어야 로컬에서 동작함(플레이스홀더로 미리 넣어둠). **Vercel에 배포할 때도 동일한 키를 프로젝트 환경변수에 추가해야** 배포본에서도 탈퇴 기능이 작동함. 이 키는 RLS를 완전히 우회하는 매우 민감한 키라 `.env.local`에만 두고 절대 커밋/공유하면 안 됨
  - 개인정보처리방침 3·5번, 이용약관 9조 문구도 "문의처로 요청" → "사이드바에서 직접 탈퇴 가능"으로 갱신

### 산 색상 (2026.07.08)
- [x] **버그 수정**: KDC 코드가 없는 책의 산 색이 산책기록/완등기록을 오갈 때 바뀌던 문제 — `getTheme()`이 kdc 없을 때 foreground 배열 안 순번(index)으로 색을 정해서, 페이지마다 배열 구성·순서가 달라 같은 책도 색이 바뀌어 보였음. 깃발 색(`getFlagColor`)과 같은 방식으로 `book.id` 해시 기반 결정론적 색 선택으로 변경 — 이제 kdc 유무·페이지와 무관하게 같은 책은 항상 같은 색

### 산책기록 목록 정렬 (2026.07.08)
- [x] **버그 수정**: "잠시 멈춤 → 읽는 중"처럼 상태만 바꿨을 때 목록 맨 위로 안 올라오던 문제 — 기존엔 등록일(`created_at`) 기준으로 정렬해서, 오래전에 등록해둔 책은 오늘 막 상태를 바꿔도 등록일이 오래됐다는 이유로 목록 아래에 그대로 남아있었음. `books` 테이블에 `status_changed_at`(신규 컬럼, "지금 이 상태가 된 시각") 추가해서, `addBook`/`changeStatus`/`updateProgress`(완독 자동 전환 시) 호출마다 `now()`로 갱신하고, 산책기록의 읽는 중/잠시 멈춤 목록은 이 값 기준 최신순으로 정렬. **DB 마이그레이션 필요**:
  ```sql
  alter table books add column if not exists status_changed_at timestamptz;
  ```
  완독(완등기록) 목록은 원래부터 `completed_at` 기준으로 이미 최신순이라 이번 변경 대상 아님(그대로 정상 동작).

IA 재구성 논의(홈/독서/등산 → 지도·리스트 통합안, 별도 대화에서 진행 중)와는 별개로, 아직 결정 안 된 디테일들을 남겨둠.

### 책 데이터
- [x] 페이지 수 미입력 시 기본값 150쪽 적용 — `app/dashboard/books/actions.ts`의 `addBook`에서 `total_pages` 미입력 시 150 저장, `AddBookForm.tsx` placeholder도 안내 문구로 교체 (2026.07.08)
- [x] 캐릭터가 오르내리는 높이/비율 — 확인 결과 현재 계산으로 충분하다고 판단, 추가 조정 불필요로 결정 (2026.07.08)
- [x] 완독한 책 길이를 km로 환산해 등반 거리로 표현 — 신국판(152×225mm) 기준 1쪽 = 225mm로 확정. 홈 통계를 "산책 거리(km)" 누적 카드 1개로 반영 (2026.07.08)
- [ ] 읽는 중인 책이 4권 이상일 때 화면 구성 미정 (PRD상 Free 플랜은 동시 3권 제한이 있었지만, 그 이상일 때 UI 표시 방식 자체는 정해진 바 없음)

### 메뉴 이름 변경 (2026.07.08)
- [x] 사이드바 라벨 변경 — '독서' → '산책기록', '등산' → '완등기록' (`Sidebar.tsx`)
- [x] 페이지 안쪽도 맞춰서 변경 — `books/page.tsx` 제목 "독서 기록"→"산책기록", `hikes/page.tsx` 제목 "등산 기록"→"완등기록" + 빈 상태 문구, `AddHikeForm.tsx` 모달 제목 "등산 기록 추가"→"완등기록 추가". 라우트 경로(`/dashboard/books`, `/dashboard/hikes`)는 아직 그대로임

### 홈 — 전경/배경 산 분리 (2026.07.08)
- [x] WorldMap을 "전경(3~5개, 또렷하고 클릭 가능)"과 "배경(흐릿한 설산 실루엣, 식별 불가)"으로 분리
  - 읽는 중인 책은 항상 전경. 부족분은 미시작(paused) 책 중 방문마다(= `books` prop이 바뀔 때) 랜덤으로 뽑아 전경을 채움(목표 4개)
  - 전경에 못 든 미시작 책은 배경 설산으로 — 픽셀아트가 아니라 매끈한 삼각형 실루엣 + 단일 안개톤이라 전경과 시각적으로도 구분됨. 겹쳐도 상관없게 설계
  - 완독 책은 정상에 깃발 꽂힌 채로 잠깐 전경에 남았다가 WorldMap에서 완전히 제외(완등기록으로만 남음) — **처음엔 24시간 유예였는데, "산책기록 지도에 완독한 책이 계속 남아있지 않고 바로 빠졌으면 좋겠다"는 피드백으로 완등 세레모니(폭죽) 재생 시간(약 3.5초)만큼만 남도록 대폭 축소** (2026.07.08 재조정)
- [x] `books` 테이블에 `completed_at` 컬럼 추가 — **DB 마이그레이션 실행 완료** (Supabase SQL Editor, 2026.07.08). `addBook`/`updateProgress`/`changeStatus`에서 완독 처리 시 `completed_at`을 기록하도록 코드도 반영됨
  ```sql
  alter table books add column if not exists completed_at timestamptz;
  ```
- [x] ~~아직 안 된 것~~ → 아래 "페이지 구조 재편" 항목에서 해결: `/dashboard/hikes`가 이제 `books`(완독) 데이터를 직접 읽어 정상석으로 보여줌
- 참고: 완등 세레모니(8비트 CLEAR 이펙트, 폭죽, 캐릭터 정상 댄스)는 이후 구현 완료 — 아래 "Phase 6~7" 항목 참고

### 홈 — 해/별 책 추가 버튼 (2026.07.08)
- [x] 하늘 우측 상단 해(낮)/별(밤)에 호버 시 "책 추가하기" 힌트, 클릭 시 추가 폼이 열리는 히든 기능 추가
- [x] 동시에 눈에 띄는 "+ 책 추가" 버튼도 홈 화면 상단에 배치 — 둘 다 같은 상태를 공유해서 어느 쪽으로 열어도 동일한 폼(검색/바코드 스캔)이 뜸
- [x] `AddBookForm`을 제어/비제어 겸용으로 확장 (`open`/`onOpenChange` prop 추가) — `/dashboard/books` 기존 사용처는 그대로 비제어로 동작
- 참고: 산이 많아 가로 스크롤이 필요한 경우, 해/별은 캔버스 콘텐츠 우측 끝 기준이라 스크롤해야 보일 수 있음. 이 경우에도 상단의 "+ 책 추가" 버튼은 항상 고정 위치라 안정적으로 쓸 수 있음

### 산 식별 (2026.07.08)
- [x] 완독/멈춤 산도 탭 가능하게 확장 — 탭하면 "제목 · 상태" 말풍선이 뜸(읽는 중인 산은 기존대로 진행률 모달이 바로 열림). 같은 KDC 색이 겹쳐도 탭 한 번으로 어떤 책인지 확인 가능
- [x] 마우스 호버로도 말풍선 표시 — 데스크톱에선 클릭 없이 마우스만 올려도 상태 무관하게 제목 팝업이 뜨도록 `mousemove`/`mouseleave` 추가. 터치 기기는 기존 탭 동작 유지
- [x] 산 아래 책 제목이 쭉 나열되던 범례(legend) 삭제 — 탭 툴팁으로 대체돼서 중복이라 제거 (`showLegend` prop, `kdcCategoryLabel` 헬퍼 함께 정리)

### 산책기록 통계 카드 교체 (2026.07.08)
- [x] 상단 통계 카드 2개를 "완등 기록/산책 거리 km" → "내가 산 책/발걸음 수"로 교체. 완독 통계는 완등기록 페이지로 넘어갔으니 산책기록에서는 뺌
- [x] "내가 산 책" = 읽는 중 + 잠시 멈춤 책 수 합산
- [x] "발걸음 수" = 읽는 중 + 잠시 멈춤 책의 `current_page` 합산 (1페이지 = 1걸음, km 환산 없이 직접 매핑)
- [x] `changeStatus`/`updateProgress`/`deleteBook`/`addBook` 액션에 `revalidatePath('/dashboard/hikes')` 추가 — 완독 처리 시 완등기록도 즉시 갱신되도록. 각 업데이트에 에러 로깅도 추가(`completed_at` 컬럼 마이그레이션 누락 등으로 조용히 실패하는 문제 디버깅용)

### 페이지 구조 재편 — 산책기록/완등기록 2메뉴 통합 (2026.07.08)
- [x] **홈(`/dashboard`)과 구 산책기록(`/dashboard/books`) 통합** — 이제 `/dashboard` 하나가 "산책기록". 상단엔 기존 홈 콘텐츠(WorldMap + 완등기록/산책거리 통계), 하단엔 기존 산책기록 콘텐츠(읽는 중 / 잠시 멈춤 `BookCard` 목록)를 그대로 이어 붙임. **완독 책은 이 목록에서 제외**(완등기록으로 이동)
- [x] `/dashboard/books`는 `/dashboard`로 리다이렉트만 하는 페이지로 축소 — 기존 링크/북마크 방어용
- [x] **완등기록(`/dashboard/hikes`) 재설계** — 더 이상 별도 `hikes` 테이블을 안 씀. `books`에서 `status = 'completed'`인 것만 불러와 상단엔 `WorldMap`을 `mode="trophy"`로 렌더링(산/나무/모닥불/깃발만 보여주고 캐릭터·책 추가 버튼·전경/배경 랜덤 선정은 전부 비활성화, 완독 책 전부를 항상 정상석으로 표시), 하단엔 완독 책 `BookCard` 목록
- [x] `WorldMap.tsx`에 `mode?: 'home' | 'trophy'` prop 추가 — trophy 모드에서는 노출 유예 없이 모든 완독 책이 항상 뜨고, 모닥불도 항상 표시, 빈 상태 문구도 "완독하면 여기에 정상석이 생겨요 🚩"로 분기
- [x] 사이드바 메뉴 2개로 축소 — 산책기록(`/dashboard`) / 완등기록(`/dashboard/hikes`), 기존 '홈' 항목 제거
- [x] `AddHikeForm.tsx`·`app/dashboard/hikes/actions.ts`(`addHike`/`deleteHike`) 코드 삭제 — 다른 곳에서 참조 없음 확인 후 제거 (2026.07.08)
- ⚠️ DB의 구 `hikes` 테이블 자체는 아직 안 지움 — 코드에서는 완전히 참조 안 하지만, 테이블 삭제는 되돌리기 어려우니 원하시면 Supabase 대시보드에서 직접 `drop table hikes;` 실행 필요
- [x] `docs/overview.md`의 페이지 구조 설명을 산책기록/완등기록 2메뉴 구조로 갱신 (2026.07.08)

### 등반 / 정상석
- [ ] "등산 기록" 명칭 → "산책 기록"으로 변경 검토
- ~~완독한 책 제목이 정상석에 새겨지도록~~ — **진행 안 함으로 결정** (2026.07.08)
- [ ] 정상석 모양을 어떻게 다르게 할지 (책별 / KDC별 / 랜덤) 미정

### WorldMap
- [x] **버그 수정**: `components/worldmap/WorldMap.tsx`의 `fixedHour` 기본값을 `10`(오전 10시 고정) → `null`(실제 시각 사용)로 변경. 데모 스크린샷이 필요하면 호출부에서 `fixedHour={10}`처럼 직접 넘기면 됨 (2026.07.08)
- [x] **메모 말풍선** — 메모 있는 책 중 날짜 시드로 2~3개만 뽑아 산 위에 항상 떠 있는 말풍선(amber 톤, 제목+메모 일부)으로 표시. 하루 동안은 같은 책이 뽑히고 자정 지나면 다시 섞임. 마우스 올린 산은 기존 제목·상태 툴팁이 우선(중복 방지로 숨김). `WorldMapBook.memo` 필드 추가, `dashboard/page.tsx`·`hikes/page.tsx` 쿼리 매핑에도 반영 (2026.07.08)

### 비즈니스 모델
- [ ] **보류**: 유료 정책(Freemium 제한, Premium 구독가, 아이템샵 등)은 아직 결정할 수 없음. `product-direction.md`의 가격·제한 수치는 PRD 초안일 뿐 확정 아님 — Phase 8 착수 전 재논의 필요

### About 페이지 (2026.07.08)
- [x] 사이드바 하단 "산책또산책에 대하여" 버튼 → 모달로 소개 문구 노출 (`AboutModal.tsx`). 별도 페이지로 분리할지는 미결정 — 내용이 늘어나면 라우트 분리 검토
- [x] **문구 전면 교체 (2026.07.08)** — 초안(옮긴이/출판사 관련 문구)에서 실제 소개 문구로 교체. 탄생 배경(등산하는 아버지·독서하는 딸), 등산과 독서의 공통점, "덧붙이는 말"(여러 권 동시 읽기 습관, 개발자가 비개발자라는 점 + 클로드와 함께 만든 점) 구성
- [ ] 메뉴/소개 전체 국문·영문 완전 이중 지원. "About" 대신 "알립니다", "주인장 백" 같은 한국어 표현 사용
- [ ] 현재 문구 (모달에 그대로 반영함)
  > 산책또산책은 산을 오르는 아버지에게 영감을 받아 탄생한 공간이에요. 등산하는 아버지와 독서하는 딸이 서로의 접점을 차곡차곡 쌓아가는 일이라 할 수 있어요.
  >
  > 산을 오르는 법과 책을 읽는 법에는 닮은 점이 있어요. 나의 현재 상태를 살피고 한 걸음씩 천천히 내딛어야 한다는 거예요. 무리해선 안되죠. 완독과 완등의 순간도 기쁘지만, 오늘 읽어 내려간 한 페이지 만큼의 발걸음으로도 충분하니까요. 이제 우리 마스코트 마루와 함께 산책을 떠나볼까요? 책 추가 버튼을 눌러보세요.
  >
  > \<덧붙이는 말\>
  >
  > 산책또산책의 개발자는 여러 권의 책을 동시에 읽어요. (노래는 한 곡을 질릴 때까지 반복해서 듣지만요.) 읽다보면 서로 다른 책들이 하나의 주제, 하나의 소재 혹은 뜻밖의 장소로 연결되는 선물 같은 순간을 만나게 돼요. 책 사이를 산책하며 숨은 재미를 발견할 수 있다면 좋겠어요.
  >
  > 사실 개발자는 웹을 전혀 모른답니다. 클로드와 함께 서툴게도 이 공간을 만들었어요. 기대에 미치지 못하더라도, 업데이트가 늦어지더라도 열린 마음으로 천천히 기다려주세요. 저도 노력하고 있답니다. 칭찬을 먹고 자라는 초보 개발자라 "잘한다" 해주시면 참 기쁠 것 같아요.

---

## 🆕 최근 작업 (2026.06.29)

- [x] **인수인계 / Phase 1 점검** — Node v22 설치, Next.js 14.2.35 구조, Supabase 연동 모두 기존재 확인. Phase 1은 사실상 완료 상태로, Vercel 배포 연결만 미확정.
- [x] **`docs/design-style.md` 보강** — 디자인 토큰, 반응형/브레이크포인트, 상태별 UI(로딩/빈/에러), 모션·접근성(prefers-reduced-motion, 색맹 대비, 스크린리더), 다크모드 정책, 로케일/카피 톤 섹션 추가.
- [x] **`docs/design-style.md` 모순 정리** — 실제 `WorldMap.tsx`가 이미 Canvas로 재구현됐는데 문서는 "현재 SVG → 목표 Canvas"로 서술돼 있어, 낡은 SVG 차이표·저채도 색상표·시스템폰트 서술 등 코드와 배치되는 내용 삭제/수정.

### ⚠️ Git 미커밋 경고
현재 작업물 대부분이 아직 커밋되지 않음 (`git log`에 `api/`, `dashboard/`, `components/`, `docs/` 없음). 마지막 커밋은 `add supabase google auth`. **작업 유실 방지를 위해 커밋·푸시 필요.**

---

## ✅ 완료된 것들

### 인프라 / 인증
- [x] Next.js 14 App Router 프로젝트 세팅
- [x] Supabase 연동 (PostgreSQL DB + Auth)
- [x] Google OAuth 소셜 로그인 (`/login`)
- [x] OAuth 콜백 처리 (`/auth/callback`)
- [x] 로그아웃

### 레이아웃 / 내비게이션
- [x] 사이드바 (`Sidebar.tsx`) — 홈 / 독서 / 등산 탭
- [x] 대시보드 레이아웃 (`dashboard/layout.tsx`)
- [x] **홈 통계 카드 축소** — 기존 "읽는 중/완독/등산 횟수/총 거리" 4개 → "완등 기록/산책 거리(km)" 2개로 교체 (hikes 테이블이 아닌 books 데이터 기반) (2026.07.08)
- [x] **홈 하단 "읽는 중"/"최근 등산" 미리보기 섹션 삭제** — `dashboard/page.tsx`에서 제거, `hikes` 쿼리도 더 이상 안 쓰여서 함께 정리 (2026.07.08)

### 독서 기록 (`/dashboard/books`)
- [x] Supabase `books` 테이블 연동
- [x] 상태별 그룹 노출 (읽는 중 / 완독 / 잠시 멈춤)
- [x] 책 카드 (`BookCard.tsx`) — 진행률 바 + 페이지 업데이트
- [x] 책 추가 폼 (`AddBookForm.tsx`) — 제목 검색 (카카오 API) + 결과 드롭다운
- [x] 바코드 스캐너 (`BarcodeScanner.tsx`) — ZXing, 실시간 ISBN 감지
- [x] API 라우트 (`/api/books/search`) — 카카오 제목 검색 + Open Library ISBN 조회
- [x] Server Actions: `addBook`, `updateProgress`, `deleteBook`

### 등산 기록 (`/dashboard/hikes`)
- [x] Supabase `hikes` 테이블 연동
- [x] 등산 추가 폼 (`AddHikeForm.tsx`)
- [x] 요약 통계 바 (총 거리 / 총 시간 / 최고 고도)
- [x] 등산 카드 목록
- [x] Server Actions: `addHike`, `deleteHike`

### 월드맵 (`WorldMap.tsx`)
- [x] 픽셀 산 렌더링 (4단계 높이 — 페이지 수 기반)
- [x] KDC 기반 색상 테마 4종 (mystery / earth / nature / fantasy)
- [x] 시간대별 하늘 색상 변화 (새벽/낮/황혼/밤)
- [x] 별 렌더링 (야간)
- [x] 태양 렌더링 (주간/황혼)
- [x] 캐릭터 2비트 바운스 애니메이션 (읽는 중)
- [x] 모닥불 애니메이션 (야간 + 읽는 중)
- [x] 완독/미시작 산 흐림 처리 (opacity 0.5, scale 0.82)
- [x] 정상석 (completed 시 정상에 회색 픽셀 바위)
- [x] 가로 스크롤 (산이 많을 때)
- [x] **산 5~10개일 때 간격 압축** — 컨테이너 폭에 맞춰 슬롯 폭을 줄이고(필요하면 겹치게) 최대한 많이 한 화면에 보이게 함. 10개 넘으면 압축 포기하고 기존 스크롤 방식으로 복귀. 최소 슬롯 폭 100px 이하로는 안 좁아짐(`MIN_SLOT_W`) (2026.07.08)
- [x] **HTML5 Canvas + requestAnimationFrame 재구현** (2026.06.29)
- [x] **산 색상 고채도 교체** — mystery/earth/nature/fantasy 원색 계열 (2026.06.29)
- [x] **픽셀 구름 오브젝트** — 주간 좌측 흐름 애니메이션 (2026.06.29)
- [x] **산 클릭 → 수직형 진도 입력 모달** — 게이지 드래그, 능선 위 캐릭터 (2026.06.29)
- [x] **BookCard 상태 변경** — 드롭다운으로 reading/paused/completed 전환 (2026.06.29)
- [x] **완독 자동 처리** — `current_page >= total_pages`시 자동 completed (2026.06.29)
- [x] **책 메모 기능** — BookCard 메모 입력/저장 (DB memo 컬럼 추가) (2026.06.29)
- [x] **캐릭터 옷 색상 → 산 KDC 테마별 보색** — 고정 파란색 대신, 각 테마(`KDC_THEME`)에 `char` 필드로 보색을 하드코딩(예: 회청색 산엔 주황 옷, 황토색 산엔 파란 옷)해서 산에 묻히지 않고 대비되게 함 (2026.07.08)
- [x] **모닥불 위치 랜덤화** — 항상 산 정중앙이 아니라 책 id 기준 고정 랜덤 위치(좌/우 치우침 4종)로 배치 (2026.07.08)

---

## 🔧 구현됐지만 목표와 차이나는 것들

| 항목 | 현재 상태 | 목표 |
|------|---------|------|
| 앱 진입점 | `/dashboard` (사이드바 레이아웃) | **`/`가 메인 월드맵 Canvas 풀스크린** |
| 도서 API | ~~카카오~~ → 국립중앙도서관 API + Open Library로 전환 완료(단 KDC 필드는 매핑에서 아직 미추출) | ✅ 달성 |
| Z 레이어 시각 | opacity + scale만 | 드롭 섀도 (전경) / 안개 레이어 (원경) |
| 로그인 | Google OAuth만 | ~~카카오 로그인 추가~~ — 진행 안 함으로 결정 (2026.07.08) |
| 페이지 구조 | `/dashboard/*` | `/`, `/mypage`, `/shop` 구조로 개편 |

> 위 항목들은 `design-style.md`에 설계 방향이 문서화돼 있으나, 코드 구현은 미완. 표 내용은 코드 기준 여전히 유효.

---

---

## 8단계 개발 로드맵 (목표 전체 계획)

로드맵 출처: PRD v1.1 기반 기획 문서

| Phase | 작업 내용 | 예상 기간 | 주요 기술 |
|-------|---------|---------|---------|
| **1** | 개발 환경 설정 (Node.js, VS Code, Next.js, Vercel 배포 연결) | 0.5일 | Next.js, Vercel |
| **2** | Supabase 설정 + 카카오/구글 소셜 로그인 + 로그인/회원가입 UI | 1일 | Supabase Auth |
| **3** | 도서 등록 — 국립중앙도서관 API 연동, 도서 검색 UI, 등록 플로우 | 1~2일 | 국립중앙도서관 API, Next.js API Routes |
| **4** | 픽셀 월드맵 — **HTML5 Canvas 렌더링**, 산 높이 시스템, KDC 자동 채색, 시간대 배경 | 3~5일 | HTML5 Canvas, JavaScript |
| **5** | 캐릭터 애니메이션 + 진도 입력 — 바운스 애니메이션, 수직 모달 UI, 실시간 슬라이딩 | 2~3일 | Canvas Animation |
| **6** | 완등 세레모니 + 정상석 갤러리 — CLEAR 이펙트, 폭죽 파티클, 갤러리 그리드 | 2일 | Canvas, CSS |
| **7** | 이미지 내보내기 — 1:1 PNG 저장, 와이드 파노라마 캡처, 워터마크 삽입 | 1~2일 | Canvas toDataURL |
| **8** | 토스페이먼츠 결제 연동 + 프리미엄 기능 게이팅 | 2일 | 토스페이먼츠 SDK |

---

## 목표 앱 구조 (로드맵 기준)

```
[Vercel — Next.js 14]
├── /              → 메인 픽셀 월드맵 (Canvas 풀스크린)
├── /login         → 카카오 / 구글 소셜 로그인
├── /mypage        → 마이페이지 + 정상석 갤러리
├── /shop          → 픽셀 아이템샵
└── /api/
    ├── /api/books    → 국립중앙도서관 API 프록시 (CORS 우회)
    └── /api/payment  → 토스페이먼츠 결제 웹훅 처리

[Supabase 클라우드]
├── Auth      → 카카오/구글 소셜 로그인 처리
├── Database  → 도서 기록, 읽기 진도, 유저 정보, 정상석 데이터
└── Storage   → 커스텀 픽셀 에셋 (캐릭터 스킨, 테마 이미지)
```

현재 구현(`/dashboard/*` 중심)은 이 구조와 다름. 향후 라우트 재편 필요.

---

## 🚧 미구현 — Phase 3~4 (핵심 비주얼 교정)

### [Phase 4] 월드맵 Canvas 재구현 ✅ 완료
- [x] **HTML5 Canvas 기반으로 WorldMap 재작성**
- [x] **산 색상 고채도로 교체**
- [x] **픽셀 구름 오브젝트 추가**
- [x] **산 클릭 → 수직형 진도 입력 모달** (능선 위 캐릭터 슬라이딩)
- [ ] **전경 드롭 섀도** (읽는 중 산이 앞으로 튀어나오는 느낌)
- [ ] **시간 슬라이더** — 하단 드래그 + Snap-back

### [Phase 3] 도서 API 교체
- [x] **국립중앙도서관 API 연동** (`/api/books/search`, `fetchNL`) — 제목/ISBN 검색 모두 국립중앙도서관 서지정보(SEOJI) API 우선 사용, 페이지 수 없을 때만 Open Library로 보완
  - [x] 총 쪽수 확보
  - [x] **KDC 데이터 추출 완료 (2026.07.12)** — `fetchNL`이 `KDC`/`SUBJECT` 필드를 추출해 `addBook`까지 전달. 위 "최근 작업 (2026.07.12)" 참고
- [ ] **중복 ISBN 방어** — Shake 애니메이션 + 픽셀 말풍선

---

## 🚧 미구현 — Phase 5 (인터랙션)

- [x] **전경 산 터치 → 수직형 진도 입력 모달** (수직 게이지 바 + 능선 위 캐릭터 슬라이딩)
- [x] **BookCard 상태 변경** — 드롭다운으로 reading/paused/completed 전환
- [x] **완독 시 자동 상태 전환** — `current_page >= total_pages`이면 `completed`
- [x] **책 메모 입력** — BookCard 메모 textarea + 저장
- ~~**카카오 로그인 추가**~~ — **진행 안 함으로 결정** (2026.07.08)

---

## 🚧 미구현 — Phase 6~7 (세레모니 + 공유)

- [x] **완등 세레모니** — 처음엔 풀스크린 팝업(`CelebrationOverlay.tsx`)으로 만들었다가, "월드맵 산 위에서 직접 터지길 기대했는데 팝업이 너무 짧게 뜨고 만다"는 피드백을 받고 **WorldMap 캔버스 인라인 이펙트로 전면 재구현**(팝업 컴포넌트는 삭제). `WorldMap.tsx`가 books prop 변화를 감지해(이전 상태와 diff, 최초 마운트 시엔 트리거 안 함) 어떤 책이 막 완독됐는지 자동으로 찾아내고, 그 책의 산 정상 위에서 3.2초간 폭죽 파티클(22개, 시간 기반 궤적) + "CLEAR!" 픽셀 타이포(팝인) + 양팔 번쩍 든 정상 댄스 캐릭터를 직접 그림. 이 동안 깃발은 잠깐 숨겨졌다가 세레모니가 끝나면 원래대로 노출. `ProgressModal`/`BookCard`는 더 이상 팝업을 직접 띄우지 않고, `ProgressModal`은 완독 저장 시 월드맵이 바로 보이도록 모달만 살짝 뒤(0.65초) 자동으로 닫음 (2026.07.08)
- ~~**정상석 솟아오르기** — 책 제목 + 완독일 음각~~ — **진행 안 함으로 결정** (2026.07.08)
- ~~**정상석 갤러리 페이지** (`/mypage`)~~ — **진행 안 함으로 결정** (2026.07.08)
- [x] **바이럴 캡처** — 1:1 PNG (인스타 피드) + 와이드 파노라마 + 워터마크 (→ 상세 기획: `docs/features/viral-capture.md`) — **완료 (2026.07.12)**, 위 "최근 작업 (4)" 참고
- [x] **CSV 내보내기** — `/api/books/export` GET 라우트, 사이드바 하단 "CSV 내려받기" 링크 (로그인 유저 본인 책 전체, UTF-8 BOM 포함) (2026.07.08)
- [x] **유저 닉네임 설정** — **완료 (2026.07.12)**, 위 "최근 작업 (2)" 참고

---

## 📋 미구현 — Phase 8 (비즈니스 모델) ⚠️ 정책 미정

> 아래 항목은 PRD 초안 수치일 뿐, 실제 유료 정책은 아직 결정되지 않음 (2026.07.08). 착수 전 재논의 필요.

- [ ] **토스페이먼츠 결제 연동** (`/api/payment` 웹훅)
- [ ] **Freemium 제한 로직** — Free: 동시 3권, 연간 24권, CSV 10개
- [ ] **Premium Ridge 패스** (₩4,900/월)
- [ ] **Pixel Item Shop** (`/shop`) — 캐릭터 스킨, 모닥불 커스텀, 갤러리 테마
- [ ] **Supabase Storage 연동** — 커스텀 에셋 저장
- [ ] **회원 탈퇴 및 데이터 Hard Delete**

---

## 다음 작업 권장 순서

```
0. ⚠️ [긴급] 현재 작업물 Git 커밋 + 푸시 (대부분 미커밋 상태 — 유실 위험)
1. ✅ [Phase 4] WorldMap → HTML5 Canvas 재구현 + 색상 고채도 교정 + 구름 추가
2. ✅ [Phase 5] 산 터치 → 수직형 진도 입력 모달 + BookCard 상태/메모
3. [Phase 1] Vercel 배포 연결 (계정 로그인 필요 — 미확정)
4. ✅ [Phase 3] 국립중앙도서관 API 교체 (단, KDC 필드 매핑은 남음)
5. ✅ [Phase 6] 완등 세레모니 (CLEAR! 이펙트 + 폭죽 + 정상 댄스, WorldMap 인라인 방식)
6. ~~[Phase 7] 정상석 갤러리 (/mypage)~~ — 진행 안 함으로 결정. PNG 내보내기는 미착수
7. [Phase 8] 토스페이먼츠 + 프리미엄 게이팅 (정책 미정, 보류)
```

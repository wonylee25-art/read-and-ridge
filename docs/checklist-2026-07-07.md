# 점검 체크리스트 (2026-07-07)

확실히 넘어가도 되는 항목은 체크하고, 애매하거나 손대야 하는 건 비워두면 됨.

## 0. 저장 (제일 급함)

- [ ] Git 커밋 + 푸시 — 샌드박스에서 대신 못 해줌, 본인 터미널에서 직접:
      `git add -A && git commit -m "wip" && git push`

## 1. 로그인 — 코드는 문제 없음

- [ ] 구글 로그인 실제로 눌러서 로그인/로그아웃 되는지 확인

## 2. 도서 기록

- [ ] Supabase 테이블에 `isbn` 컬럼 있는지 확인 (없으면 중복 방지·등록이 에러 남)
- [ ] Supabase 테이블에 `memo` 컬럼 있는지 확인 (없으면 메모 저장이 에러 남)
- [ ] 같은 책 두 번 등록 → 흔들림 + "이미 책장에 있어요" 메시지 뜨는지 실제 테스트
- [ ] 페이지 수 안 넣고 등록한 책 → 진행률 0% 고정되는 거 그대로 둘지, 기본값(300쪽) 넣을지 결정

## 3. 등산 기록 — 문제 없음

- [ ] (점검 결과 이상 없음, 그냥 넘어가도 됨)

## 4. 픽셀 월드맵

- [ ] `WorldMap` 기본값이 "오전 10시 고정"이라 실제 화면엔 밤하늘·별·모닥불이 절대 안 뜸 — 실시간으로 되돌릴지 확인
- [ ] 신비 테마 색상 회색으로 이미 반영됨 — 원하는 색 맞는지 화면으로 최종 확인만
- [ ] 완독/미시작 산이 흐릿하게 보이던 효과가 삭제되고 전부 선명하게 바뀜 — 의도한 변경인지 확인

## 앞으로 할 일 (로드맵)

- [ ] Vercel 배포 연결
- [ ] 국립중앙도서관 API로 교체 (도서 API)
- [ ] 카카오 로그인 추가
- [ ] 완등 세레모니 (CLEAR 이펙트 + 정상석)
- [ ] 정상석 갤러리 페이지 + PNG 내보내기
- [ ] 토스페이먼츠 결제 연동

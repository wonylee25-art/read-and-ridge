// 공개 지형도 관련 공용 상수.
//
// ⚠️ 서버 액션 파일('use server')은 async 함수만 export할 수 있어서 상수를 거기 둘 수 없다.
// 그리고 클라이언트 컴포넌트(BookCard)도 이 값을 쓰기 때문에, 서버 전용 모듈
// (lib/trail/public-books.ts — 관리자 클라이언트를 import한다)에 둬서도 안 된다.
// 그래서 의존성 없는 별도 파일로 뺐다.

// 맞춰보세요 책에 붙일 수 있는 힌트 개수 상한.
// 하나로는 "너무 쉽거나 너무 어렵거나"가 되기 쉽고, 넷 이상이면 사실상 정답 공개라 3개.
export const MAX_QUIZ_HINTS = 3

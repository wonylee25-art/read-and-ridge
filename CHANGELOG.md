# Changelog

산책또산책(Read & Ridge)의 버전별 변경 이력. 배포(push)할 때마다 여기에 기록하고,
`package.json`의 `version`과 `lib/version.ts`의 `LAST_UPDATED`도 같이 갱신할 것.

## 0.6.0 — 2026-09-20 (36)

공유가 **한 바퀴 돌게** 만든 버전. 0.5.0에서 남이 들어올 주소는 생겼는데, 방문자가
놀다 가도 주인 쪽에는 아무 일도 일어나지 않았다. 방문 → 반응 → 주인이 다시 열어볼
이유까지가 이번 범위다. 새로 들어온 사람이 막히는 자리와, 기록하러 다시 들어오는
길(홈 화면 아이콘)도 함께 손봤다.

- **다녀간 사람들** — `components/dashboard/GuestLog.tsx`, `lib/trail/guest-log.ts`,
  `lib/josa.ts`, `app/dashboard/page.tsx`.
  `guesses` 테이블은 0.5.0부터 방문자의 답을 전부 쌓고 있었지만 **주인이 그걸 보는
  화면이 없었다** — 데이터는 계속 쌓이는데 아무도 안 봤다. 산책기록 지도 아래에
  최근 12개를 싣는다.
  - **숫자로 보여주지 않는다.** "조회수 37"은 늘지 않으면 실망하고 늘면 집착하는
    값이고, `social-visit.md` 5절이 안 만들기로 한 것들과 같은 성격이다. 대신
    "누가 뭐라고 답했는지"라는 사건으로 보여준다 — 비교할 대상이 없고, 사람 이름과
    문장이 남는다. 틀린 답도 함께 싣는다(대개 그쪽이 더 재밌다).
  - 아무도 안 왔으면 **구역 자체를 그리지 않는다.** "아직 아무도 다녀가지 않았어요"
    같은 빈 상태는 공개 링크를 켤 마음을 꺾는다.
  - 조회는 서비스 롤이 아니라 로그인 세션(RLS)으로 한다 — `guesses`에는 주인만 읽는
    SELECT 정책(`auth.uid() = owner_id`)이 이미 걸려 있어서 새 정책도 마이그레이션도
    필요 없었다.
  - 이름을 안 남긴 방문자는 '누군가'. 조사는 받침을 보고 고른다(`lib/josa.ts`) —
    "『사피엔스』을(를)"처럼 괄호를 달면 문장이 사무적으로 읽힌다. '으로/로'는
    ㄹ 받침 예외까지 본다("서울로", "물로").

- **막히는 자리의 힌트** — `components/dashboard/HintBubble.tsx`, `app/dashboard/page.tsx`.
  처음 들어온 사람을 위한 안내. 화면을 흐리게 덮고 3~4장을 순서대로 넘기는 방식은
  쓰지 않았다 — ① 이 앱은 "설명 대신 예시 지형도를 먼저 보여준다"는 선택을 이미 했고
  그 위에 오버레이를 덮으면 스스로 고른 방식을 가리는 셈이고 ② 사람이 막히는 지점은
  기능을 못 찾아서가 아니라 **아직 책이 없어서**다(산이 0개인 화면에서 "여기를 누르면
  진도를 입력해요"라고 가리켜봐야 가리킬 산이 없다). 그래서 한꺼번에가 아니라 그
  안내가 실제로 필요해지는 순간에 한 줄씩 뜬다.
  - 비로그인 첫 진입 / 책은 있는데 아무 산도 안 오른 상태 / 첫 완등 직후, 세 자리.
  - 세 번째 힌트가 **공개 링크의 발견성**도 같이 메운다 — 공개 토글이 산책자 증표
    안에 묻혀 있어 있는 줄 모르고 쓰는 경우가 많았다.
  - 한 번 닫으면 그 기기에서 다시 안 뜬다(localStorage). 비로그인도 보는 화면이라
    계정이 아니라 브라우저에 기억시킨다.

- **맞히면 이름표가 넘어간다** — `app/trail/[slug]/{TrailClient,GuessModal}.tsx`,
  `lib/trail/revealed.ts`, `components/worldmap/WorldMap.tsx`, `app/globals.css`.
  정답을 맞혀도 화면에 아무 변화가 없어서, 방문자가 한 일이 어디에도 안 남았다.
  이제 물음표 팻말이 제목 팻말로 젖혀진다.
  - **완등 폭죽을 재사용하지 않는다.** 완등은 주인이 몇 주에 걸쳐 한 권을 다 오른
    성취고 맞히기는 3초짜리다. 같은 연출을 두 번 쓰면 폭죽이 "잘했다"가 아니라
    "뭔가 일어났다"는 뜻이 되고 완등이 싸진다. 여기서는 **상태 변화 자체가 연출**이다.
  - 연출의 무대는 모달이 아니라 지도여야 해서, 정답 화면의 버튼을 '닫기'에서
    **'지도에서 보기'**로 바꾸고 그걸 누를 때 이름표를 넘긴다.
  - 드러난 제목은 **그 방문자의 브라우저에만** 남는다. 서버에 저장하면 ① 뒤에 온
    사람은 놀 게 없고 ② 주인이 '맞춰보세요'로 둔 의도가 첫 방문자 한 명에게 지워진다.
    정답 여부는 어차피 `guesses`로 주인에게 전달되므로 화면의 이름표는 각자의 기억으로
    충분하다.
  - 제목 길이 임계값은 `maskTitle`이 이미 보존하고 있어서 **이름표가 바뀌어도 산
    모양은 그대로**다. 쪽수는 가린 값을 유지한다 — 맞혔다고 주인의 정확한 진도까지
    풀 이유는 없다.
  - 정답 문구를 "정상 도착!"에서 **"이름표를 달았어요"**로. 은유를 새로 만들지 않고
    이 화면이 이미 쓰는 말('이름표')을 그대로 썼다. 창 제목이 "◯◯님이 산 책은
    무엇일까요?"인 경우가 있어 아래 줄의 닉네임은 뺐다(같은 말이 두 줄 반복됐다).
  - 버그: 이름표 메모(`nameplateItems`)의 의존성이 책 id만 봐서, 제목이 바뀌어도
    **옛 팻말이 그대로 남았다**. 제목·isQuiz까지 넣어야 갱신된다.

- **공개 지도에서 산을 누르면 책 정보** — `lib/trail/public-books.ts`,
  `app/trail/[slug]/TrailClient.tsx`.
  제목 한 줄짜리 검은 띠 대신 제목·저자·상태·진행률(쪽수 + 막대)을 보여준다. 놀러 온
  사람이 가장 궁금한 건 "이 책 뭐지"이고, 거기서 "나도 읽어볼까"로 넘어간다.
  - **가려진 책에는 저자를 내보내지 않는다.** 제목을 가려놓고 저자를 주면 검색 한
    번에 끝나서 맞히기가 성립하지 않는다. 완독해서 이름표가 드러난 책은 `quizHidden`이
    false라 자연스럽게 함께 공개된다.
  - `PUBLIC_BOOK_COLUMNS`에 `author`를 넣으면서 "select('*') 금지" 주석도 갱신 —
    author는 일부러 넣은 것이고 `cover_url`은 여전히 안 내보낸다는 걸 명시.
  - 표지는 넣지 않았다. `cover_url`이 DB에 **한 건도 채워져 있지 않다** — 띄우려면
    등록 파이프라인에서 먼저 채워야 한다.

- **홈 화면에 추가 (PWA)** — `app/manifest.ts`, `app/layout.tsx`, `public/icon-*.png`.
  기록하러 들어오는 길이 "브라우저 → 주소창 → 산책또산책 → 산 누르기"라, 며칠 안 열게
  되는 이유의 상당 부분이 앞쪽 세 단계에 있었다. 아이콘이 생기면 한 번으로 줄어든다.
  - `start_url`은 `/`가 아니라 `/dashboard` — `/`는 어차피 리다이렉트라 띄울 때마다
    한 번 더 돌 이유가 없다.
  - iOS 사파리는 manifest를 보지 않으므로 `apple-touch-icon`과 `appleWebApp` 설정을
    따로 건다.
  - 아이콘은 앱 팔레트(`KDC_THEME.earth` + 하늘 그라데이션 + 정상 깃발)로 32×32
    픽셀아트를 그려 최근접 확대. 배경(gray-50)과 같은 `background_color`로 띄우는
    순간 흰 화면이 번쩍이지 않게 했다.
  - 두 손가락 확대는 막지 않았다 — 픽셀 산을 들여다보는 화면이라 확대가 필요하다.

## 0.5.0 — 2026-09-20 (35)

혼자 쓰는 기록 도구에서 **남에게 보여줄 수 있는 기록**으로 넘어간 버전. 그동안 공유
수단은 PNG 캡처 하나뿐이었는데, 이미지를 받은 사람이 들어올 **착지점**이 없었다
(`docs/features/social-visit.md` 레벨 1). 0.4.x 후반에 만들어 놓고 이 파일에 기록하지
못한 것들을 여기서 함께 정리한다.

- **공개 등반지도 `/trail/[slug]`** — `app/trail/[slug]/{page,TrailClient}.tsx`,
  `lib/trail/{public-books,slug}.ts`, `lib/supabase/admin.ts`, `profiles` 테이블.
  로그인 없이 열리는 읽기 전용 지형도. 산책기록·완등기록 지도와 누적 숫자를 보여준다.
  기본값은 **비공개**이고, 공개는 산책자 증표의 토글을 켜는 명시적 행동으로만 시작된다.
  - 링크는 사람당 하나(`/trail/<12자>`). slug는 암호학적 난수에서 뽑고 닉네임·id에서
    파생하지 않는다 — 파생하면 남의 slug를 추측해 "이 사람도 공개했나"를 열거할 수
    있고, **공개 여부 자체가 사적인 정보**다. 눈으로 읽거나 불러줄 일이 있어서
    혼동하기 쉬운 글자(0/o, 1/l/i)는 알파벳에서 뺐다. 토글을 끄면 slug가 지워져 기존
    링크가 즉시 404.
  - 닉네임 원본은 계속 `auth.users.user_metadata`에 두고, 남이 읽어야 하는 값만
    `profiles`에 복제한다(미러 실패가 닉네임 저장 자체를 막지는 않는다).
  - **유출 차단이 이 기능의 핵심.** 공개 페이지는 클라이언트 캔버스라 props로 넘긴 건
    전부 페이지 소스와 RSC 페이로드에 남는다 — "안 그린 것"이 아니라 **"안 넘긴 것"만**
    안전하다. 조회는 `select('*')` 대신 명시 컬럼 목록(새 컬럼은 기본적으로 비공개),
    메모는 모든 책에서 제거, isbn은 아예 안 내보내고 오로라 판정 결과만 전달,
    `completed_at`은 날짜까지만 자른다(방문자 화면에서 완등 세레모니·인증샷 버튼이
    뜨지 않게).
  - `lib/supabase/admin.ts`에 `cache: 'no-store'`를 못 박음. Next가 서버 fetch를
    가로채 캐시하는 탓에 **책을 비공개로 바꿔도 공개 페이지에 계속 보이는** 문제가
    실제로 발생했다. 페이지에 `force-dynamic`을 걸어도 fetch 캐시는 별개라 안 풀렸다.

- **맞춰보세요 — 이름표를 뗀 산** — `app/trail/[slug]/{GuessModal,guess-actions}.tsx|ts`,
  `lib/trail/{public-books,constants}.ts`, `books.quiz_hints`.
  책별 공개 범위를 '맞춰보세요'로 두면 산은 보이되 제목이 가려지고, 방문자가 무슨
  책인지 맞혀볼 수 있다. 완독하면 자동으로 공개된다.
  - 제목은 고정 길이 물음표로, 쪽수는 `PAGES_PER_STEP` 배수로 가린다. 둘 다 산의
    실루엣·크기를 주인 화면과 **픽셀 단위로 동일하게** 유지하면서 글자 수·정확한
    쪽수는 감추는 값이다. (얇은 책의 쪽수가 0으로 반올림돼 250쪽 기본값으로 커지던
    버그도 여기서 잡음.)
  - 맞히기 서버 액션은 검사를 빠뜨리면 **모든 사용자의 모든 책 제목을 확인해주는
    오라클**이 된다. visibility·slug 일치·완독 여부·길이 제한을 순서대로 검사하고,
    제목은 정답일 때만 돌려준다. IP 기준 간단한 레이트 리밋도 함께.
  - 힌트 쪽지는 최대 3장(`quiz_hint` text → `quiz_hints` text[] 마이그레이션, 기존
    값은 첫 칸으로 이관). 한꺼번에 늘어놓지 않고 누를 때마다 한 장씩 넘긴다 — 다
    보여주면 너무 쉬워지고 화면도 힌트가 다 차지한다. 힌트는 앱이 만들어주지 않는다.
    "작년에 네가 추천한 그 작가" 같은 사람이 쓴 힌트가 훨씬 재밌기 때문.

- **주인 쪽 공개 설정** — `components/dashboard/{ProfileModal,VisibilityModal}.tsx`,
  `components/books/BookCard.tsx`, `app/dashboard/books/actions.ts`.
  읽는 책 목록은 사적이라, 숨기고 싶은 책 한 권 때문에 공유 자체를 포기하게 된다.
  그래서 책별 공개 범위가 공유 링크보다 먼저 필요했다.
  - 책 카드의 아이콘을 누를 때마다 공개(지구본) → 맞춰보세요(물음표) → 비공개(자물쇠)
    순으로 돈다. 소장 토글과 같은 조작감·같은 낙관적 업데이트.
  - 한두 권이 아니라 "숨길 것부터 전부 정리"하려면 카드를 하나씩 찾아 눌러야 해서,
    산책자 증표에 일괄 설정 모달을 따로 뒀다. 전체 일괄 지정 + 책별 3단 선택, 실제로
    바뀐 것만 모아 visibility 종류당 쿼리 한 번씩(최대 3번)으로 저장한다.

- **이름표 팻말 — 어느 산이 어느 산인지** — `components/worldmap/WorldMap.tsx`,
  `worldmap-utils.ts`, `app/trail/[slug]/TrailClient.tsx`.
  공개 지형도의 전경 산 꼭대기에 제목 팻말을 상시로 세운다. 가려진 책은 제목 대신
  보라색 물음표 팻말. 전에는 마우스를 올려야만 제목이 떠서, **터치 기기로 링크를 받은
  사람에겐 모든 산이 똑같은 이름 없는 산**이었다 — 머리말이 "이름표가 없는 산은
  눌러보세요"라고 말하는데 정작 어느 산에도 이름표가 없었던 것. 주인 화면은 그 자리를
  메모 말풍선이 쓰고 어차피 자기 책이라 켜지 않는다(`nameplates` prop).
  캔버스 양 끝 팻말은 밖으로 넘어가 잘려서, 한글 11px 기준으로 폭을 어림해 안쪽으로
  밀어 넣는다.

- **공개 등반지도 화면 정리** — `app/trail/[slug]/{page,TrailClient}.tsx`.
  제목을 "◯◯님의 등반지도"로 하고, 숫자 세 칸(산책 수 / 발걸음 수 / 완독거리)을 지도
  위로 올렸다. 놀러 온 사람이 먼저 보고 싶은 건 사용법이 아니라 **그 사람이 얼마나
  걸었나**여서, 설명 문단은 오른쪽 상단 (?) 모달로 접었다. 발걸음 수는 공개 지형도에
  없던 값이라 새로 싣되 주인 화면과 같은 공식을 쓴다.

- **지형도 좌우 화살표가 실제로 눌린다** — `components/worldmap/WorldMap.tsx`.
  0.4.23에서 넣은 가장자리 화살표는 `pointer-events-none`인 **표시**였고, 손가락으로
  쓸어넘기는 걸 전제했다. 그런데 데스크톱은 휠이 세로로만 굴러서(스크롤바도 숨긴
  상태) 화살표가 보여도 옆으로 넘길 방법이 사실상 없었다. 이제 누르면 한 화면의
  80%씩 넘어간다. 그라데이션 띠는 그대로 클릭을 통과시키고 아이콘만 눌린다.

- **완등기록 지형도 높이 축소** — `components/worldmap/constants.ts`, `WorldMap.tsx`.
  440 → 300 (모바일 352 → 264). 가장 큰 산도 땅까지 172px이라 나머지가 전부 빈
  하늘이었는데, 산책기록과 달리 완등기록은 그 공간을 채우는 요소(메모 말풍선·이름표
  팻말)가 없다. 구름은 y=25~60에만 떠서 낮아진 높이에서도 산과 겹치지 않는다.
  완독 맵 PNG 캡처는 자기 높이(440)를 따로 쓰므로 영향 없음.

- **맞히기·공개 범위 문구를 산책 톤으로** — 맞히기 창 제목에 닉네임을 넣고 산마다
  다른 문구가 뜬다("◯◯님이 지금 오르고 있는 산은?"). 책 id로 고정 배정이라 같은 산은
  언제 눌러도 같은 질문이고, 읽는 중/잠시 멈춤에 따라 문구 묶음이 다르다. 기존 문구는
  사무적이었고 무엇보다 **누구의 산인지가 문장에서 빠져** 있었다 — 놀러 온 사람
  입장에서 주인공은 그 지형도의 주인인데. 공개 범위 설명도 안개/이름표 비유로 통일
  (공개=이름표를 달아둠, 맞춰보세요=이름표를 떼고 수수께끼, 비공개=안개에 가림).

- **WorldMap 읽기 전용 표면** — `components/worldmap/WorldMap.tsx` (동작 변화 없음).
  남의 지형도를 렌더링하기 위한 준비. `readOnly`(PNG 캡처 UI를 숨겨 남의 기록을
  이미지로 내려받지 못하게), `onBookTap`(상태와 무관하게 모든 산을 탭 대상으로 —
  기존 `onBookClick`은 "읽는 중인 책만"이라는 계약을 `WorldMapClient`가 의존하고
  있어 건드리지 않고 분리), `WorldMapBook.aurora`(공개 지형도는 isbn을 클라이언트로
  내보낼 수 없어 판정 결과만 미리 계산해 싣는다).

## 0.4.25 — 2026-09-19 (34)

- **소장 중인 책에 산기슭 베이스캠프(텐트 + 모닥불) 표시** —
  `components/worldmap/{constants,drawing,WorldMap,capture}.ts(x)`,
  `worldmap-utils.ts`, `lib/demo-books.ts`. 사용법 문서에는 "소장 중인 책은 산기슭에
  불이 켜집니다"로 적혀 있었지만 실제로는 `owned` 값이 지형도에서 **한 번도 쓰인 적이
  없었음**(2026.07.12에 확인하고 `docs/ideation.md`로 옮겨둔 항목). 모닥불은 '읽는 중 +
  야간' 또는 완등기록(trophy) 모드일 때만 켜져서 소장 여부와 무관했다.
  이제 `WorldMapBook`에 `owned`를 싣고, **소장 중인 책에만** 산기슭에 텐트를 세운다 —
  낮에는 텐트만, 밤(별 뜨는 시간대)에는 텐트 입구에 불이 켜지고 옆에 모닥불이 함께 탄다.
  읽기 상태(읽는 중/완독/잠시 멈춤)와는 무관하게 소장 여부만 본다. 빌린 책·도서관
  대출본에는 캠프가 생기지 않아 "잠시 들른 산"과 대비된다.
  - 완등기록(trophy)과 완독 맵 PNG 캡처도 같은 규칙으로 통일 — 기존엔 완독 책 전부에
    모닥불이 켜졌지만 이제 소장 중인 책에만 켜진다. 단 trophy/캡처는 시각과 무관하게
    항상 '밤 캠프'(점등 + 모닥불)로 그려 완등을 자축하는 기존 연출을 유지.
  - 검토했던 "소장 표시를 여러 기호로 다양하게" 안은 기각 — 지형도가 이미 산 높이·KDC
    색·캐릭터 위치·깃발·하늘·날씨·식생으로 정보 과밀이라, **텐트/모닥불 = 소장**이라는
    단일 기호로 통일하는 쪽이 공유용 캡처에서도 읽힌다고 판단.
  - 텐트 한 칸 크기(`TENT_BLOCK`)는 3에서 4로 올림 — 3이면 실제 표시 배율에서 텐트인지
    알아볼 수 없었음(브라우저 실측).
  - 천막 색은 `TENT_PALETTES` 4벌(샌드 / 민트 / 로즈 / 하늘) 중 책마다 고정된 하나.
    처음엔 주황 계열 한 벌뿐이라 바로 옆 모닥불과 같은 색이어서 텐트가 아니라 횃불처럼
    보인다는 피드백이 있었고, 이어서 한 벌로는 단조롭다는 피드백으로 4벌이 됐다.
    어느 벌이든 주황·노랑은 배제하고(불꽃과 구분), 산 팔레트(청회색·황토·청록·연보라)와
    잔디도 피했다. **테두리(능선)와 내부를 색으로 나누지 않고 한 벌당 단색 한 가지**로
    칠한다(피드백) — 그래서 낮 배경과 밤 배경 양쪽에서 다 읽히도록 너무 옅지 않은
    중간 톤으로 잡았고, 입구만 어둡게 파서 텐트로 읽히게 했다. 밤 등불 색은 4벌 공통.
  - 캠프 위치 계산을 `geometry.ts`의 `getCampLayout()`으로 분리하고 연속값으로 바꿈.
    비율 4종(0.12/0.28/0.68/0.82) 중 하나를 고르던 방식은 산이 몇 개만 있어도 캠프가
    죄다 비슷한 자리에 서 있는 것처럼 보였음("산마다 위치도 달리 해줘"). 이제
    ① 좌/우 기슭 중 한쪽 ② 그 구간 안의 연속 위치 ③ 모닥불을 산 바깥쪽에 두는
    방향 ④ 천막 색(팔레트 인덱스)까지 책마다 고정된 랜덤으로 정한다(정중앙은 비워둬서 오르는 캐릭터와 안 겹침).
    본 렌더와 PNG 캡처가 같은 함수를 쓰므로 두 경로의 캠프 자리가 항상 일치한다.
- **오로라 이스터에그 트리거 책 추가 (9월·10월)** — `lib/aurora-books.ts`.
  - 9월: 『호킹 주식회사』(엘렌 미알레, 김연화 옮김, 동녘사이언스, ISBN 9788990247940)
  - 10월: 『기계가 언어를 사용한다는 것에 대한 인문학적 사유』(마크 코켈버그·데이비드 J.
    건컬, 신동숙 옮김, 생각이음). 이 책은 서지 DB에 ISBN이 9791198740748 /
    9791198740755 두 건으로 잡히고 앱 검색창에도 둘 다 뜨므로, 사용자가 어느 쪽을
    골라도 걸리도록 **두 ISBN 모두** 등록.
  - 매달 한 권씩 고른다는 운영 규칙과 "한 번 넣은 ISBN은 빼지 않는다"(이미 등록해 둔
    사용자의 깃발 색이 바뀌므로)는 원칙을 파일 주석으로 남김.

## 0.4.24 — 2026-08-22 (33)

- **모바일에서 진척도 게이지를 끌면 뒤 페이지가 같이 스크롤되던 문제** —
  `components/worldmap/ProgressModal.tsx`. `touchmove` 리스너가 `{ passive: true }`로
  등록돼 있어 `preventDefault()`를 할 수 없었고, 브라우저가 같은 손가락 움직임을 페이지
  스크롤로도 함께 처리했음(피드백: "책 진척도 스크롤을 올렸다 내리면 뒤에 페이지도 같이
  오르락 내리락해"). 마우스 경로에는 없는 문제라 PC에선 안 보였음. ① 게이지 요소에
  `touchAction: 'none'`을 줘서 게이지 위에서 시작한 터치를 브라우저가 스크롤로 쓰지
  않게 하고, ② `touchmove`를 `{ passive: false }`로 바꿔 드래그 중일 때만
  `preventDefault()` — 손가락이 게이지 밖으로 벗어난 뒤에도 스크롤이 따라붙지 않는다.
  `touchcancel`도 함께 처리해 터치가 끊겨도 드래그 상태가 남지 않게 함.
  (모달 바깥 어두운 영역을 끄는 경우는 여전히 뒤 페이지가 움직일 수 있음 — body 스크롤
  잠금은 iOS에서 스크롤 위치가 튀는 부작용이 있어 이번엔 건드리지 않음.)

## 0.4.23 — 2026-08-22 (32)

- **지형도 터치 조작 개선** — `components/worldmap/WorldMap.tsx`. 모바일 사용이
  불편하다는 피드백으로 실제 기기 폭(375px)에서 확인한 세 가지를 고침.
  ① 캔버스 폭이 864px인데 스크롤바를 숨겨둬서(`scrollbarWidth: 'none'`) 지도의
  60%가 화면 밖인 걸 알 수 없었음 → 남은 방향 가장자리에 그라데이션 + 화살표를
  띄우고 끝에 닿으면 감춤(스크롤 컨테이너 바깥에 두어 같이 밀려나지 않게 함).
  ② 지도를 끌면 마지막에 click이 따라와 손 뗀 지점의 산/책추가 버튼이 눌렸음
  (실제로 로그인 화면으로 튕김) → pointer 이벤트로 8px 이상 이동을 드래그로 보고
  뒤따르는 click 한 번을 무시. ③ 터치 기기는 탭할 때 mousemove를 흉내내지만
  mouseleave가 없어 산 말풍선이 사라지지 않았음 → 호버 처리를
  `(hover: hover) and (pointer: fine)`일 때만 하고, 지도 바깥을 누르면 닫음.

- **모바일 지형도 높이 축소** — `components/worldmap/constants.ts`, `WorldMap.tsx`.
  440px 고정이라 모바일에서 화면 세로의 54%를 차지해 통계 카드가 밀렸음. 좁은 화면
  (`COMPACT_MAX_W = 768` 미만)에서만 `CANVAS_H_COMPACT = 352`를 쓴다(54% → 43%).
  산 크기는 그대로 두고 빈 하늘만 덜어내는 방식이라 픽셀아트가 뭉개지지 않음.
  PC는 440 유지 — 세로 공간이 넉넉하고 오로라 연출을 그대로 두기 위함.

- **오로라 하늘 상한 자동 계산** — `components/effects/AuroraOverlay.tsx`,
  `constants.ts`. `SKY_ROWS_LIMIT = 24`가 하드코딩이라 캔버스 높이를 바꾸면 오로라가
  산을 가로지르는 구조였음. `getSkyRowsLimit(canvasH)`가 땅·최대 산 높이를 빼서
  계산하고 prop으로 전달 — canvasH=440이면 24로 기존과 동일, 352면 16(160px)이라
  산 꼭대기(180px)보다 20px 위에서 멈춤. AuroraOverlay가 WorldMap 상수를 직접
  import하지 않는 기존 설계는 유지(기본값 24).

## 0.4.22 — 2026-08-22 (31)

- **OS 다크모드에서 입력칸 글씨가 안 보이던 문제 수정** — `app/globals.css`.
  `@media (prefers-color-scheme: dark)`가 `--foreground`를 `#ededed`로 바꿨는데,
  화면 배경은 흰색 고정이라 Windows 다크모드에서 body 글자색을 물려받는
  `input/select/textarea`(Tailwind preflight의 `color: inherit`)가 흰 배경 위
  밝은 회색으로 렌더돼 책 추가 폼에 입력한 글자가 보이지 않았음(피드백: "책을
  적는 칸에 글씨가 회색으로 떠서 글자가 안 보여"). 앱은 원래 라이트 전용이므로
  다크모드 오버라이드를 제거하고, 입력 요소 글자색(`#171717`)과
  placeholder 색(`#9ca3af`)을 명시. 추가로 `:root`에 `color-scheme: light`를
  선언해 브라우저 기본 위젯(날짜 선택기, select 드롭다운, 체크박스, 스크롤바,
  자동완성 배경)도 OS 테마를 따라가지 않도록 고정.

## 0.4.21 — 2026-08-01 (30)

- **오로라 이스터에그 트리거 책 추가** — `lib/aurora-books.ts`. `AURORA_ISBNS`에
  『래퍼와 공원』(땅 시리즈 3, 송재홍, 민음사, ISBN 9788937492556) 추가. 이 책을
  갤러리에 등록하면 기존 『세계문학전집 이야기』와 동일하게 10초짜리 오로라 연출이
  재생되고, 완독 후 정상 깃발도 오로라 팔레트로 고정됨.

## 0.4.20 — 2026-07-12 (29)

- **완등기록 산 겹침 방지** — `components/worldmap/geometry.ts`, `WorldMap.tsx`.
  `computeSlotW`가 산 개수만 보고 슬롯 폭을 압축해서, 폭이 넓은 산(쌍봉·큰 스텝)이
  섞인 채로 컨테이너가 좁으면 압축된 슬롯 폭이 실제 산 폭보다 작아져 옆 산과 몸통이
  겹쳐 보이는 문제가 있었음(피드백: "겹치는 게 너무 커"). `computeSlotW`가 이제
  `count` 대신 `books` 배열을 받아, 이번에 그릴 산들 중 실제 최대 산 폭 + 최소
  여백(PX 1칸)을 압축 하한에 반영 — 겹침 자체를 막고, 그래도 안 맞으면 압축을
  포기하고 가로 스크롤로 넘김. 호출부 4곳(`WorldMap.tsx` 3곳, `getMountainRects`)
  전부 갱신.
- **산 전체 크기 배율 추가 축소 (0.75 → 0.6)** — `components/worldmap/constants.ts`.
  v0.4.19의 0.75배로도 여전히 크다는 피드백 — `SIZE_SCALE`을 0.6으로 낮춰 기존
  대비 약 20% 추가 축소. `MAX_STEPS`도 12 → 10으로 같이 줄어듦.

## 0.4.19 — 2026-07-12 (28)

- **산 전체 크기 0.75배 축소** — `components/worldmap/constants.ts`, `geometry.ts`.
  v0.4.18에서 연속 스케일링으로 바꾸면서 최대 스텝이 11→16으로 늘어난 여파로
  산이 전체적으로 너무 커 보인다는 피드백. `getSteps()`가 raw 스텝을 구한 뒤
  `SIZE_SCALE`(0.75)만큼 한 번 더 줄이도록 변경 — 높이(steps)를 줄이면 폭
  (numCols = 2·steps±1)도 같은 비율로 줄어들어 산 전체가 비율 그대로 축소됨.
  `MAX_STEPS` 16→12로 재계산, `MAX_MTN_W`도 함께 축소(330→250). 212쪽/304쪽
  구간별 높이 차이(7/9스텝)는 그대로 유지됨.

## 0.4.18 — 2026-07-12 (27)

- **산 높이 계산을 4단계 구간 → 페이지 수 연속 스케일링으로 변경** —
  `components/worldmap/geometry.ts`, `constants.ts`. 기존 `getLevel()`은
  200/400/600쪽 경계로만 4단계(5/7/9/11스텝)를 나눠서, 같은 구간에 속하면
  두께가 달라도(예: 212쪽 vs 304쪽, 둘 다 200~399쪽) 산 높이가 완전히 똑같아
  보이는 문제가 있었음(사용자 피드백: "산 크기가 별로 차이가 안나네"). `getLevel`을
  `getSteps(pages)`로 교체 — `PAGES_PER_STEP`(40쪽)마다 스텝 +1인 연속식으로
  바꿔 `MIN_STEPS`(4)~`MAX_STEPS`(16) 사이로 클램프. 212쪽→9스텝, 304쪽→12스텝
  (기존엔 둘 다 7스텝)으로 30px(3스텝) 차이가 나도록 함. `STEPS_BY_LEVEL` 상수와
  `MountainVisual.level` 필드는 제거(다른 곳에서 참조하지 않던 값). `MAX_MTN_W`도
  새 `MAX_STEPS` 기준으로 재계산해 슬롯 폭 압축·완독맵 파노라마 레이아웃이
  최대 산 크기와 계속 맞도록 조정.

## 0.4.17 — 2026-07-12 (26)

- **산책기록 설명문 수정** — `components/books/AddBookBar.tsx`. "읽고 있는 책만
  지도에 산으로 솟아나요."를 "추가한 책은 지도에 산으로 솟아나요. 잠시 멈춘
  책은 설산이 됩니다."로 변경 — 등록만 해도 전경/배경 어딘가에 산으로 나타난다는
  실제 동작(WorldMap 전경/배경 로직)에 맞춰 문구 정정.

## 0.4.16 — 2026-07-12 (25)

- **비로그인 예시 지형도 TUTORIAL 라벨 모바일 중앙 정렬 수정** —
  `components/worldmap/WorldMap.tsx`, `drawing.ts`, `app/globals.css`. 캔버스에
  직접 그리던 방식(`drawTutorialLabel`)은 산이 많아 캔버스가 뷰포트보다 넓어지는
  모바일 화면에서 캔버스 콘텐츠 중앙과 실제 화면 중앙이 어긋나는 문제가 있었음.
  스크롤 안 되는 바깥 컨테이너(wrap) 기준 HTML 오버레이로 교체해 스크롤 위치·
  캔버스 폭과 무관하게 항상 실제 화면 정중앙에 오도록 수정. 반짝이는 효과는
  CSS `@keyframes tutorial-flicker`로 재현(기존 sin 파형과 유사한 느낌).

## 0.4.15 — 2026-07-12 (24)

- **회원 탈퇴 경고 문구 보강** — `components/dashboard/DeleteAccountModal.tsx`.
  "이 작업은 되돌릴 수 없어요." 뒤에 "개발자도 복구할 수 없습니다."를 추가해
  탈퇴가 정말로 되돌릴 수 없는 작업임을 한 번 더 강조.

## 0.4.14 — 2026-07-12 (23)

- **"내가 산 책" 통계를 등록된 책 전체 기준으로 변경** — `app/dashboard/page.tsx`,
  `app/dashboard/layout.tsx`. 예전엔 완독 통계가 완등기록 페이지로 옮겨가면서
  "내가 산 책"도 읽는 중+잠시 멈춤만 세고 완독을 제외했는데, "내가 산 책"은 등록한
  책 전체를 세는 게 맞다는 피드백으로 완독 포함 전체 등록 책 수로 되돌림(산책기록
  페이지 상단 통계 카드·"산책자 증표" 팝업 통계 둘 다 동일 기준으로 통일).
- **`WorldMap.tsx` 리팩토링(동작 변경 없음)** — 2,128줄 한 파일에 있던 상수·좌표
  계산·캔버스 드로잉·날씨·PNG 캡처 로직을 역할별로 분리: `constants.ts`(상수·픽셀
  데이터), `geometry.ts`(좌표/크기/시드 계산), `sky-weather.ts`(하늘·날씨 상태),
  `drawing.ts`(캔버스 드로잉 함수), `capture.ts`(PNG 내보내기). `WorldMap.tsx`엔
  이제 컴포넌트(상태 관리 + 애니메이션 루프 + JSX)만 남음(834줄). 특히
  `baseX = 24 + i*slotW + (MAX_MTN_W-mtnW)/2` 좌표 공식이 4곳 넘게 복붙돼 있던 걸
  `getMountainVisual()`/`getStripBaseX()` 두 함수로 통합(`docs/verification.md`
  "🔧 리팩토링 대상" 항목 처리). 값·로직은 전부 원본 그대로 옮겨왔고 화면 결과는
  동일함 — `npx tsc --noEmit`, `npx eslint .` 전체 통과 확인.

## 0.4.13 — 2026-07-12 (22)

- **산책기록 월드맵 저장 시 책 제목·메모 함께 출력** — `components/worldmap/WorldMap.tsx`
  `handleCaptureHomeWorldMap`. 라이브 화면은 일부러 제목·메모를 늘 그리지 않는데(탭
  툴팁/오늘의 랜덤 말풍선으로만 노출), 저장한 PNG에도 그대로 안 보여 아쉽다는 피드백.
  내보내기 순간에만 라이브 루프와 동일한 위치 계산(`computeSlotW`/`MAX_MTN_W`)으로
  각 전경 산에 제목(항상)+메모(있는 책만)를 한 번 덧그린 뒤 캡처하도록 수정 —
  `toDataURL()`까지 동기 처리라 다음 rAF 프레임이 끼어들지 않고, 화면 자체는 다음
  프레임에 평소 모습으로 복귀해 깜빡임 없음.

## 0.4.12 — 2026-07-12 (21)

- **산책기록 카메라 기본 동작 분리** — `components/worldmap/WorldMap.tsx`. 좌하단
  카메라 버튼의 평소(방금 완독한 책이 없을 때) 탭 동작이 모드와 무관하게 항상
  "완독 맵"(완독한 책만 모은 파노라마, `handleCaptureCompletedMap`)이라, 산책기록
  (home) 페이지에서 눌러도 완독맵만 나와 혼란스럽다는 피드백. 완등기록(trophy)은
  기존 동작을 유지하고, 산책기록에서는 새 `handleCaptureHomeWorldMap`이 지금 화면의
  캔버스를 `canvas.toDataURL()`로 그대로 저장하도록 분리(읽는 중+완독+배경 산·날씨
  전부, 파일명 `산책또산책_산책기록월드맵_...`). 버튼 노출 조건도 trophy는
  `completedBooks.length > 0`, home은 `books.length > 0`으로 나눔(완독한 책이 없어도
  산책기록 월드맵은 저장할 수 있어야 하므로). 버튼 title/aria-label도 모드별로 분기.

## 0.4.11 — 2026-07-12 (20)

- **산-책 제목 간격 확보** — `components/worldmap/WorldMap.tsx`의 `drawMountainTitle()`.
  산 밑동(지면 시작선)과 책 제목 텍스트가 너무 붙어 보인다는 피드백으로 y 오프셋을
  7px → 12px로 소폭 확대(닿지 않을 정도로만). 산책기록 인증샷의 완독일 텍스트도
  같은 간격을 유지하도록 20px → 25px로 함께 밀어냄. 완등기록 파노라마(같은 함수 공유)에도
  동일하게 반영됨.

## 0.4.10 — 2026-07-12 (19)

- **정상 인증샷 산 크기 3/4로 축소** — `components/worldmap/WorldMap.tsx`
  `renderCompletionCapture()`. "정상이 세로 절반까지" 목표치도 여전히 크다는 피드백으로,
  최종 배율에 `HERO_SCALE_ADJUST = 0.75`를 곱해 그 목표치의 3/4만 적용(최소 1배는
  유지). 옆산·해도 같은 `zoom` 값을 공유하므로 함께 줄어듦.

## 0.4.9 — 2026-07-12 (18)

- **정상 인증샷 산 정상 = 캔버스 1/2 지점 목표 + 옆산 잘림 허용** — `components/worldmap/WorldMap.tsx`
  `renderCompletionCapture()`. 여전히 하늘 여백이 크게 느껴진다는 피드백으로, 목표를
  "산 정상이 세로 캔버스 절반 지점까지 닿는 높이"로 직접 계산하도록 변경(`HERO_TARGET_HEIGHT
  = mountainBaseY - size * 0.5`). 옆산이 겹치지 않게 하던 가로 폭 상한(`HERO_MAX_WIDTH`)은
  제거하고, 옆산이 캔버스 밖으로 나가도 그리기를 멈추지 않게 해 화면 끝에서 자연스럽게
  잘리는 구도를 허용함("양옆에 산은 잘리도록").

## 0.4.8 — 2026-07-12 (17)

- **정상 인증샷 산 크기 재조정 + 옆산·해도 함께 확대** — `components/worldmap/WorldMap.tsx`
  `renderCompletionCapture()`. 직전 확대(2/3 지점 목표)가 너무 크다는 피드백으로 최종
  배율을 그 절반만 적용(`HERO_SCALE_ADJUST = 0.5`, 최소 1배 보장). 대신 이 배율(`zoom`)을
  주인공 산 혼자만이 아니라 옆산(`drawSideMountain`/`sideMountainWidth`에 `zoom` 인자
  추가)과 해(`drawPixelSun`에 `scale` 인자 추가)에도 함께 적용해 장면 전체가 조화롭게
  커지도록 함.

## 0.4.7 — 2026-07-12 (16)

- **"인증샷 찍기" 라벨 재축소** — `components/worldmap/SlideToCapture.tsx`. 직전에
  키운 `text-2xl`(24px)이 너무 크다는 피드백으로 그 절반인 `text-xs`(12px)로
  줄이고, 썸-텍스트 여백(9px→5px)·텍스트-오른쪽 여백(22px→11px)도 함께 절반으로 축소.
- **폭죽 터지는 산은 메모 말풍선 숨김** — `components/worldmap/WorldMap.tsx`.
  완독 세레모니(CLEAR!+폭죽+댄스 캐릭터)가 진행 중인 산 위에 메모 말풍선까지
  겹쳐 어수선해 보인다는 피드백으로, `justCompletedId`인 책은 `memoBubbles` 후보에서
  제외하도록 수정. 세레모니 유예시간이 끝나 `justCompletedId`가 지워지면 다시 노출.

## 0.4.6 — 2026-07-12 (15)

- **모바일 인사 문구 겹침 수정** — `components/dashboard/ProfileTrigger.tsx`. 좁은
  화면에서 페이지 제목("산책기록" 등)과 같은 줄에 절대 위치로 겹쳐 배치된
  "{닉네임}님, {시간대 인사 문구}"가 제목 텍스트와 겹쳐 보인다는 피드백
  (2026.07.12, 모바일 스크린샷)으로, `sm` 브레이크포인트 미만에서는 인사 문구를
  생략하고 "{닉네임}님"만 노출하도록 반응형 처리.

## 0.4.5 — 2026-07-12 (14)

- **"인증샷 찍기" 라벨 확대·여백 축소** — `components/worldmap/SlideToCapture.tsx`.
  라벨 글씨를 메뉴바 제목("산책기록" 등)과 같은 `text-2xl font-bold`로 키우고,
  고정 폭(220px) 트랙 대신 라벨 내용에 맞춰 늘어나는 구조로 바꿔 양옆 여백을
  기존 대비 약 1/3로 좁힘(썸-텍스트 간격 9px, 텍스트-오른쪽 끝 22px). 슬라이드
  거리는 클릭 시점에 실제 렌더된 버튼 폭을 측정해 계산하도록 변경해, 폭이
  달라져도 "밀어서 잠금해제" 애니메이션이 항상 끝까지 정확히 미끄러지게 함.

## 0.4.4 — 2026-07-12 (13)

- **정상 인증샷 산 크기 확대** — `components/worldmap/WorldMap.tsx`의
  `renderCompletionCapture()`. 원래 페이지수 기준 크기 그대로 그리면 정사각형
  캔버스 위쪽에 하늘 여백이 지나치게 많이 남는다는 피드백(2026.07.12)으로,
  캔버스 중심·지면을 고정점 삼아 주인공 산을 확대(`ctx.scale`)해 세로 프레임의
  절반~2/3 정도를 채우도록 수정. 옆산 배치·나무 장식은 확대된 산 폭(`scaledLeft`/
  `scaledRight`) 기준으로 재조정했고, 옆산이 캔버스 밖으로 넘치면 기존처럼
  안전하게 생략됨. 세레모니 이펙트(CLEAR!·폭죽·댄스 캐릭터)도 산과 함께
  확대되어 더 임팩트 있게 보이도록 했고, 책 제목·완독일 텍스트는 확대 영향 없이
  기존 크기 유지.

## 0.4.3 — 2026-07-12 (12)

- **인증샷 슬라이드 버튼 단순화** — `components/worldmap/SlideToCapture.tsx`. 정확히
  작은 원형 썸을 잡고 끝까지 드래그해야 확정되던 방식이 조작하기 번거롭다는
  피드백으로, 알약 트랙 어디를 눌러도 바로 확정되도록 단순화. 눌렀을 때 썸이
  끝까지 스르륵 미끄러지는 300ms 애니메이션만 재생해 "밀어서 잠금해제" 비주얼은
  그대로 유지. 라벨도 "밀어서 인증샷 찍기" → "인증샷 찍기"로.

## 0.4.2 — 2026-07-12 (11)

- **"인증샷 찍기" 버튼 라벨 상시 노출** — `components/worldmap/WorldMap.tsx`. 좌하단
  카메라 버튼이 강조 상태(호박색+펄스)로만 바뀌면 hover하기 전엔 무슨 버튼인지
  알아보기 어렵다는 피드백으로, 강조 상태일 때 버튼 오른쪽에 "인증샷 찍기" 텍스트
  라벨을 hover 없이 항상 띄우도록 수정.

## 0.4.1 — 2026-07-12 (10)

- **바이럴 캡처 UX 개선** — `components/worldmap/WorldMap.tsx`. 사용자 피드백 반영.
  - "인증샷 찍기" 버튼을 산 위(메모 말풍선과 겹쳐 보기 불편)에서 **좌하단 기존 카메라
    버튼**으로 통합. 방금 완독한 책이 있으면 버튼이 호박색+펄스로 강조되며 "인증샷
    찍기"로 동작, 없으면 평소대로 "완독 맵 저장"(전체 파노라마).
  - 산책기록 캡처(`renderCompletionCapture`)에서 주인공 산 옆에 다른 책 최대 3권을
    흐릿한 배경 실루엣(`drawBackgroundRange`) 대신 **축소(70%)·반투명(60%) 실제 산**으로
    보여주도록 교체(`drawSideMountain`) — 어떤 책들이 옆에 있는지 알아볼 수 있게 함.
    캔버스 폭을 벗어나는 경우는 안전하게 생략.

## 0.4.0 — 2026-07-12 (9)

- **바이럴 캡처(정상 인증샷) 구현** — `components/worldmap/WorldMap.tsx`. 그동안 기획만
  있고 코드는 없던 `docs/features/viral-capture.md` 기능을 실제로 붙임. 열린 질문 3개를
  결정하고 반영: (1) 트리거는 완독 순간 자동 팝업 대신 세레모니 유예시간 중 뜨는
  "인증샷 찍기" 버튼, (2) 폭죽 피크 프레임은 elapsed=2000ms 고정(퍼짐 최대·아직
  안 흐려짐 시점), (3) 워터마크는 "산책또산책 | 닉네임" 형식.
  - `renderCompletionCapture()` — 산책기록 캡처(1:1 정사각형). 방금 완독한 산을 중심에
    크게, 나머지 책은 흐린 배경 능선으로, CLEAR!+폭죽+정상 댄스 캐릭터를 책 id로
    시드 고정한 파티클로 재현 가능하게 그림. 책 제목+완독일만 노출(정보 절제 원칙).
  - `drawKdcBadge()`/`countByTheme()` — 왼쪽 상단 KDC 색 구성 뱃지(라벨 없이 색+숫자만).
    산책기록 캡처는 책장 전체, 완등기록 캡처(`renderCompletedPanorama`)는 완독한 산
    전체(화면 표시 캡(`TARGET_TROPHY`)과 무관) 기준으로 집계.
  - `drawWatermark()`가 `nickname` 인자를 받아 "산책또산책 | {닉네임}" 형식으로 표시.
    `lib/nickname.ts`(신규, `getNicknameFromUser()`)로 닉네임 도출 로직을 `layout.tsx`·
    `dashboard/page.tsx`·`hikes/page.tsx` 세 곳이 공유하도록 정리(중복 제거).

## 0.3.5 — 2026-07-12 (8)

- **시간대별 인사 문구 추가** — `components/dashboard/ProfileTrigger.tsx`. "{닉네임}님,
  반가워요" 고정 문구 대신, 2시간 단위 12구간(0~24시)마다 후보 2개 중 하나를 랜덤으로
  보여줌(`GREETING_PHRASES`/`pickGreetingPhrase`). 브라우저 로컬 시각 기준으로 계산해야
  해서(서버 시각을 쓰면 사용자 시간대와 어긋남) 마운트 후 `useEffect`에서 교체하는 방식 —
  서버 렌더 시점엔 기본값 "반가워요"만 잠깐 보임(WorldMap 하늘 시각 계산과 동일한 원칙).

## 0.3.4 — 2026-07-12 (7)

- **산 실루엣 다양화 구현** — `components/worldmap/WorldMap.tsx`. `design-style.md`에
  설계만 있고 코드에는 반영 안 돼 있던 기능. 책 제목이 길면(12자 이상) 폭이 가장 넓은
  쌍봉(twin) 실루엣을 배정해 산 아래 제목이 덜 잘리게 하고, 그 외에는 책 id 시드로
  뾰족(sharp)/비대칭(skew)/고원(plateau) 중 하나를 결정론적으로 골라 같은 분야 책이라도
  산 모양이 다양해지게 함(색은 그대로 KDC 분류를 나타냄). `getMountainShape`/
  `getMountainProfile`로 실루엣별 칼럼 높이맵을 계산하고, `drawMountainBody`로 본
  렌더(`WorldMap`)·PNG 파노라마(`renderCompletedPanorama`)·클릭 hit-test
  (`getMountainRects`)가 모두 같은 프로필을 공유해 실루엣이 어긋나지 않게 함.

## 0.3.3 — 2026-07-12 (6)

- **산책자 증표(프로필) 트리거 정렬 수정** — `app/dashboard/layout.tsx`에서 "{닉네임}님,
  반가워요" 트리거가 페이지 제목("산책기록"/"완등기록")보다 한 줄 위에 따로 떠 있던
  문제를 수정. children과 같은 `relative` 컨테이너 안에서 `absolute top-0 right-0`으로
  배치해 제목과 같은 줄(같은 기준선)에 오도록 정렬함.

## 0.3.2 — 2026-07-12 (5)

- **산책자 증표(프로필) 팝업 추가** — 오른쪽 상단 "{닉네임}님, 반가워요" + 사람 아이콘
  버튼(`ProfileTrigger.tsx`) 클릭 시 팝업(`ProfileModal.tsx`) 오픈. 닉네임 입력/저장,
  산책 시작일(`user.created_at`), 최근 산책일(`status_changed_at` 최댓값), 통계 카드
  4개(내가 산 책/발걸음 수/완등기록/완등거리)를 함께 보여줌. 산책기록·완등기록 양쪽
  레이아웃(`app/dashboard/layout.tsx`)에 공통 배치. 이전의 시간대별 인사말 +
  인라인 연필 편집(`Greeting.tsx`)은 폐기하고 이 팝업으로 통합(`docs/features/profile.md`).
- **최근 산책일 정확도 개선** — `updateProgress()`가 상태 변화 없이 페이지만 저장해도
  `status_changed_at`을 매번 갱신하도록 확장. 읽는 중/잠시 멈춤 목록 정렬도 함께 더
  정확해짐.
- **완등거리 환산 비율 공유** — `DISTANCE_PER_PAGE_M`을 `worldmap-utils.ts`로 이동해
  완등기록 페이지와 프로필 팝업의 거리 숫자가 어긋나지 않도록 함.

## 0.3.1 — 2026-07-12 (4)

- **닉네임 & 인사말 추가** — `auth.users.user_metadata.nickname`에 저장(새 테이블 없음).
  산책기록(`/dashboard`) 상단에 시간대별 인사말(새벽/주간/황혼/야간) 노출, `BookCard`와
  동일한 연필 아이콘 인라인 편집. 기본값은 구글 프로필 이름 자동 채움. 나중에 바이럴
  캡처 워터마크에도 재사용 예정(`docs/features/nickname.md`).

## 0.3.0 — 2026-07-12 (3)

- **비로그인 진입 화면을 "제목 + 로그인 버튼"에서 예시 지형도로 전면 개편** — 어떤
  서비스인지 설명 없이 로그인부터 요구하던 문제를 해결. `/`는 로그인 여부와 무관하게
  항상 `/dashboard`로 이동하고, `/dashboard`(산책기록)·`/dashboard/hikes`(완등기록)
  자체가 비로그인 상태를 내부에서 분기 처리(middleware 보호 해제).
  - 산책기록: 예시 책 4권(`lib/demo-books.ts`)으로 채운 월드맵을 보여주고, 산 클릭
    (게이지 체험)·소개 모달은 그대로 가능. 계정 데이터를 직접 다루는 하단 책
    목록(BookCard)은 비로그인일 땐 숨김.
  - 완등기록: 가짜 완독 예시 대신, 로그인 유저가 0권일 때 보는 것과 동일한 빈
    상태 화면(안내 문구 + EmptyState)을 그대로 재사용.
  - 책 추가 / 게이지 저장 / CSV 내려받기 / 회원 탈퇴: 비로그인 상태에서 누르면
    그 자리에서 바로 구글 로그인(`lib/auth/signInWithGoogle.ts`)으로 유도.
  - 로그아웃/회원 탈퇴 후에도 `/login`이 아니라 예시 지형도가 있는 `/dashboard`로
    이동하도록 통일.
- **사이드바 메뉴 재정비** — 순서를 "소개 → 로그인·로그아웃 → CSV 내려받기 →
  개인정보처리방침·이용약관 → 회원 탈퇴"로 바꾸고, 항목 사이 구분선을 전부 제거해
  더 단순한 리스트로 정리.
- **월드맵에 깜빡이는 TUTORIAL 픽셀 라벨 추가** — 비로그인 예시 지형도에서만
  하늘 중앙에 sin 파형으로 반짝이는 "TUTORIAL" 픽셀 글씨(CLEAR! 텍스트와 같은
  서체) + 그 아래 "로그인 후 책을 추가해보세요" 안내를 오버레이로 노출.
- **완등기록 빈 상태 문구 교체** — "완독하면 여기에 정상석이 생겨요" →
  "완등한 산이 이곳으로 옮겨옵니다"로, 지도 안 안내문/`EmptyState` 둘 다 통일.

## 0.2.2 — 2026-07-12

- **mystery 테마 색상 조정** (`WorldMap.tsx`) — 총류/철학/종교(KDC 0,1,2) 산 색이 저채도라
  화면에서 "그냥 회색"으로 보인다는 피드백으로, 청회색/슬레이트 톤이 또렷이 보이도록 조정
  (`#b9bdc1` → `#8fa4c4` 등). `docs/design-style.md` 색상표도 함께 동기화.

## 0.2.1 — 2026-07-12 (2)

- **KDC 백필 스크립트 추가** (`scripts/backfill-kdc.mjs`, `npm run backfill:kdc`) — 이미
  등록된 책 중 `kdc`가 비어있고 `isbn`이 있는 책을 대상으로 SEOJI API를 다시 조회해
  `kdc` 컬럼을 채운다. `SUPABASE_SERVICE_ROLE_KEY`로 RLS를 우회해 전체 유저 대상으로
  1회 실행. 로컬(네트워크 제한 없는 환경)에서 직접 실행 필요 — 개발 서버/배포 파이프라인에
  포함되지 않는 수동 유지보수 스크립트.

## 0.2.1 — 2026-07-12

- **KDC 산 색상 매핑**: 국립중앙도서관 서지정보(SEOJI) API 응답에서 `KDC`·`SUBJECT` 필드를
  추출해 책의 `kdc` 컬럼에 저장하도록 연결 (`/api/books/search`, `AddBookForm`, `addBook`).
  실측 확인 결과 `KDC` 필드는 CIP(사전 등록) 도서에서만 채워지고 대부분의 최신/자가출판
  도서는 비어 있어, `SUBJECT`(KDC 대분류 한 자리 숫자)를 1차 폴백으로 함께 사용.
  둘 다 없으면 기존처럼 `book.id` 해시 순환 배정으로 자연스럽게 대체됨(동작 변경 없음).
  "KDC가 산 색을 결정한다"는 핵심 컨셉의 남은 전제 조건이었던 부분(`docs/verification.md`
  ⚠️ 부분 6) 해소.

## 0.2.0 — 2026-07-11

- **책 추가 팝업**: 검색 결과 목록이 길어져도 팝업이 늘어나지 않고 목록 안에서만
  스크롤되도록 수정 (`max-h-72 overflow-y-auto`).
- **책 추가 팝업**: 저자가 여러 명이라 문자열이 아주 길어지는 경우, 자동 입력되는
  저자 값을 30자로 자르도록 수정 (`truncateAuthors`). 팝업 크기가 항상 고정되도록.
- **버전 표기**: 사이드바에 BETA 배지, 앱 버전(`vX.Y.Z`), 마지막 업데이트 날짜 표시 추가.
  버전 관리 체계 도입(`lib/version.ts`, 이 CHANGELOG).

## 0.1.0 — 2026-07-09

- 베타 오픈: 구글 로그인, RLS, Vercel 환경변수 설정 완료 후 최초 배포.
- 팝업 위치/레이아웃 버그 4건 수정 (자세한 내용은 `배포전_체크리스트.md` 참고).
